name: Sync WP → Supabase (כל הפוסטים)

on:
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * *'   # כל יום ב-04:00 UTC

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci

      - name: Sync all posts
        run: node scripts/sync-all.mjs

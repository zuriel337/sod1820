# Preview coexistence note

RoyalContextBar is deliberately retained during Human-Gate preview. The new BottomBar context slot is a parity candidate; retirement of the old admin-only bar happens only after visual/capability confirmation. No duplicate store/engine is introduced.
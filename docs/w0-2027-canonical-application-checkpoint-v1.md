# SOD1820 — W0 2027 CANONICAL APPLICATION CHECKPOINT V1

**Date:** 2026-09-09  
**Status:** DB RULE LAYER CANONICALIZED · LONG-FORM OWNER PACKAGE BRANCH-READY · MAIN/MASTER-STATE RELEASE PENDING  
**Human Gate:** ZURIEL  
**OWNER CHECK:** `EXTEND_EXISTING`

## 1. DB-LIVE canonical owner changes

Canonical Supabase `linswmnnkjxvweumprav` now has exactly one active version for each:

- `research_workspace_law` **v2 ACTIVE**; v1 preserved inactive;
- `workspace_layout_standard` **v2 ACTIVE**; v1 preserved inactive.

These changes supersede fixed shell/3-column/no-fullscreen presentation requirements while preserving Research OS/context/semantic-zone invariants and the Number/Phrase bridge.

Reproducibility migration on this branch:

`supabase/migrations/20260909024500_w0_2027_research_workspace_layout_v2.sql`

## 2. Branch-ready long-form owner package

Prepared on `gpt/roadmap-v5-4-product-map-2027`:

- `docs/sod1820-system-frame-contract-v2-addendum.md`
- `docs/research-studio-v1-2027-adaptive-addendum.md`
- updated `SOD1820_MASTER_OWNER_INDEX.md`
- `docs/w0-2027-master-state-supersession-delta-v1.md`

This package records:

- Adaptive System Frame: persistent Orientation + Global Navigation projection + Adaptive Commands + one Raziel + My Workspace;
- desktop Global Navigation may render as a collapsible Vercel-style Sidebar; mobile as drawer/sheet; keyboard as command/search projection;
- direct SEO product entry survives; Heichal is optional deep mode;
- Number/Phrase = `CONNECT NOW · REDESIGN LATER` with signature capabilities protected;
- S10.4 visual lock superseded while Discovery Engine output target survives/re-homes;
- legacy CC-1..CC-4 future UI/phasing absorbed into W5/W6, preserving Human-Gate/truth/security/intake/system-operation capabilities;
- Admin Research/Human Gate stays semantically distinct from System Operations/Metatron;
- language/access/Premium readiness remains one-identity projection architecture.

## 3. Deliberately not claimed

The following are **not** yet true:

- the long-form addenda are merged to `main`;
- `SOD1820_MASTER_STATE.md` contains the dated W0 supersession entry;
- the updated Owner Index is canonical on `main`;
- any 2027 Shell UI is implemented/deployed/live;
- W0 is fully `CANONICAL CLOSED` across DB + main + Master State.

Current precise state:

`W0 SPECIFICATION COMPLETE`  
`DB RULE OWNER LAYER = CANONICAL / LIVE`  
`LONG-FORM OWNER PACKAGE = BRANCH READY`  
`MASTER STATE ADDITIVE DELTA = PREPARED, NOT APPLIED`  
`MAIN RELEASE = NOT AUTHORIZED`  
`UI IMPLEMENTATION = NOT STARTED`

## 4. Release gate

On explicit ZURIEL release authorization (`תעלה`), the release pass must:

1. reverify current `origin/main` and parallel writers;
2. reconcile branch against current main;
3. append the prepared dated delta to `SOD1820_MASTER_STATE.md` without deleting history;
4. merge the System Frame + Research Studio addenda, updated Owner Index, Roadmap/W0 docs and reproducibility migration coherently;
5. verify canonical Supabase still has one active v2 row for both rules;
6. verify main now reproduces/documentedly agrees with the live DB owner state;
7. only then mark W0 canonical closure and move to **W0.5 Visual Foundation 2027**.

No product deploy is required merely to merge governance/docs/migration reproducibility, unless release tooling creates one automatically; deployment state must still be reported honestly.
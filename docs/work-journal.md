# יומן עבודה — SOD1820

> מסמך עבודה: תיעוד רטרוספקטיבי של מה שנבנה בפרויקט עד היום, ובסיס לרשומות עתידיות.
> נוצר ב‑11.6.2026 מתוך היסטוריית ה‑git וסריקת הקוד בפועל.

---

## 1. סקירת הפרויקט

האתר בנוי כ‑**Single Page Application** מבוסס React 18 + Vite, פרוס ב‑Vercel.
מקור הנתונים הוא **Supabase**, והתוכן נשאב מ‑WordPress (`sod1820.co.il/wp-json`) אל Supabase
ומשם מוצג באתר. עיקר הלוגיקה בקובץ אחד גדול: `src/App.jsx` (~5,387 שורות).

- **טווח הפיתוח עד כה:** 8.6.2026 → 11.6.2026 (60 קומיטים).
- **ענף פיתוח נוכחי:** `claude/work-journal-project-uujukt`.

---

## 2. יומן כרונולוגי — מה נעשה

מסודר לפי אבני‑דרך מתוך היסטוריית ה‑git (מהישן לחדש), מקובץ לשלבים.

### שלב 0 — בסיס האתר (8.6)
שרשרת קומיטים בשם "שמירה אוטומטית" — הקמת ה‑SPA הראשונית: דפי בית/בלוג/פוסט,
מערכת העיצוב (פלטת זהב/ארגמן, גופנים), והחיבור הראשוני ל‑Supabase ול‑WordPress.

### שלב 1 — מבנה האתר וכלי ELS
| קומיט | מה נעשה |
|-------|---------|
| `f7775d9` | הוספת מסמך מבנה האתר (`docs/site-structure.md`) — מיפוי קיים + מבנה מוצע. |
| `5744ffc` | דף הדגמה עצמאי לכלי ELS (`public/els-demo.html`). |
| `8b5c388` | הוספת כלי דילוגי האותיות (ELS) לדף הבית — במקום מקטע הקורסים. |
| `85d2594` | טעינת טקסט התורה המלא לכלי ELS עם חיפוש מאונדקס מהיר. |

### שלב 2 — שדרוגי כלי ELS
| קומיט | מה נעשה |
|-------|---------|
| `9c2805d` | דיוק מניין אותיות התורה — כתיב בלבד (304,850). |
| `9b5845a` | התאמה לנוסח קורן המסורתי — בדיוק 304,805 אותיות. |
| `ee30da9` | מיצוב הכלי כ"עדות ולא ניבוי". |
| `cda5eb8` | סבילות לשגיאות, חיפוש אשכול מונחים, ומיון לפי מובהקות. |
| `c46cfe6` | ציר אנכי + חיפוש פנימי בציר; הגדרות הכלי נטענות מ‑Supabase. |
| `69cea0e` | קישור עמוק (deep link) לכלי — כתובות חיפוש הניתנות לשיתוף. |

### שלב 3 — Realtime ותשתית
| קומיט | מה נעשה |
|-------|---------|
| `83b21a9` | מימוש טיפול באירועי realtime (field engine). |
| `d059df2` | תיקון מיקום `realtime-engine.js` — העברה ל‑`src/lib`. |
| `9af1996` | תיקון מטמון — `index.html` מתרענן בכל פריסה. |

### שלב 4 — גיבוי אוטומטי
| קומיט | מה נעשה |
|-------|---------|
| `db2e19b` | גיבוי יומי אוטומטי של ה‑repo דרך GitHub Actions (Release יומי) + תיעוד. |

### שלב 5 — לוח גלישה (Jetpack 2015→)
| קומיט | מה נעשה |
|-------|---------|
| `c20e653` | לוח ניהול — ייבוא היסטוריית גלישה מ‑Jetpack (2015→). |
| `1fb200f` | ציר זמן רב‑רזולוציה, מקורות תנועה וקליקים. |
| `8b7ee90` | הודעות, מנויים, ייצוא CSV, פילוח יום‑בשבוע, צמיחה שנתית + ראוט ישיר. |

### שלב 6 — ניקוי (PR #15)
| קומיט | מה נעשה |
|-------|---------|
| `b3e7eda` | הסרת מקטע הקורסים מהאתר וניקוי סביבת העבודה. |

---

## 3. מצאי תכולה — מה קיים כיום

### דפים ציבוריים (`src/App.jsx`)
| דף / רכיב | תיאור |
|-----------|-------|
| HomePage | דף הבית — Hero, ציר זמן אירועים, גלריית מספרי‑מפתח, טופס העלאת ממצאים, מקטע ELS, המלצות, פוסטים אחרונים/פופולריים. |
| BlogPage | בלוג עם חיפוש טקסט, חיפוש גימטריה, פילטרים (קטגוריה/תגית/שנה), עימוד. |
| PostPage / PostPageBySlug | צפייה בפוסט בודד עם תמונה ראשית ומטא‑דאטה. |
| AboutPage | אודות צוריאל פולייס + כרטיסי נתונים. |
| LoginPage | כניסה/הרשמה + הרשמה לדיוור. |
| ContactPage | טופס יצירת קשר. |
| ChatPage | צ'אט קהילתי בזמן אמת (Supabase realtime). |
| Timeline + LiveSignalBar | ציר זמן אירועים לפי שנה/נושא + טיקר אירועים זורם. |
| Popular Posts | פוסטים פופולריים לפי מספר תגובות. |

### כלי ELS — דילוגי אותיות (`src/App.jsx` ~470–1092)
חיפוש מונח בודד או אשכול מונחים (מופרדים בפסיק), טווח דילוג מוגדר, חיפוש דו‑כיווני,
סבילות לשגיאות (עד 2), קישור עמוק לשיתוף, תצוגת מטריצה / ציר אנכי עם חיפוש פנימי.
מקור הטקסט: `public/torah-letters.txt` (התורה המלאה, 304,805 אותיות). הגדרות מ‑`els_settings`.

### ממשק ניהול (`src/App.jsx` ~3393–4330)
| כלי | תיאור |
|-----|-------|
| עורך עמודים | עריכת תוכן לדפי בית/אודות/בלוג. |
| מנהל רמזים | הוספה/מחיקה של רמזים (localStorage) עם תמונות. |
| סנכרון | משיכת פוסטים/תגובות מ‑WordPress ל‑Supabase. |
| לוח גלישה | סטטיסטיקות Jetpack מ‑2015, ציר זמן רב‑רזולוציה, מקורות, קליקים, ייצוא CSV. |
| תיבת הודעות + מנויים | הודעות מטופס יצירת קשר, רשימת מנויים, סימון נקרא, ייצוא CSV. |
| דוח מספרים | סריקת כל הפוסטים לדפוסי מספרים והתאמה למשמעויות מפתח. |

### ספריות ורכיבים
- **`src/lib/supabase.js`** — לקוח Supabase + פונקציות שליפה/סנכרון (פוסטים, תגובות, גימטריה,
  צ'אט, תנועה, מנויים, תיבת ניהול).
- **`src/lib/realtime-engine.js`** — ערוץ realtime לטבלאות שדה (edges/discoveries/entities ועוד).
- **`src/components/UploadFindings.jsx`** — טופס העלאת ממצאי גימטריה (מילים+ערכים, תמונה, טיוטה).

---

## 4. אוטומציה ותשתית

### GitHub Actions (`.github/workflows/`)
| Workflow | תזמון | תפקיד |
|----------|-------|-------|
| `daily-backup.yml` | יומי 03:00 UTC + ידני | גיבוי ה‑repo כ‑GitHub Release בשם `backup-YYYY-MM-DD`. |
| `sync-posts.yml` | יומי 04:00 UTC + ידני | הרצת `scripts/sync-all.mjs` — סנכרון WordPress → Supabase. |

### סקריפטים (`scripts/`)
| קובץ | תפקיד |
|------|-------|
| `sync-jetpack-stats.mjs` | ייבוא סטטיסטיקות Jetpack (CSV) → `legacy_traffic` (2015→). |
| `sync-posts.mjs` | משיכת פוסטים מ‑WordPress → Supabase. |
| `sync-all.mjs` | סנכרון מלא עם טיפול בשגיאות וניסיונות חוזרים. |
| `sync-numbers.mjs` | חילוץ מספרים מתוך כותרות/תוכן פוסטים וקיטלוגם. |
| `sync-missing.mjs` | השלמת פוסטים מעמודים חסרים. |
| `fix-slugs.mjs` | פענוח סלאגים מקודדי‑URL (עברית). |
| `migrate-media.mjs` (שורש) | העברת מדיה מ‑FTP של WordPress → אחסון Supabase. |

---

## 5. מודל הנתונים (Supabase)

| טבלה | תיאור |
|------|-------|
| `posts` | פוסטים מ‑WordPress. |
| `comments` | תגובות מ‑WordPress. |
| `gematria_words` | מיפוי מילה עברית → ערך גימטריה. |
| `galleries` | גלריות תמונות לפי מספר עוגן. |
| `gallery_images` | תמונות בתוך גלריות. |
| `gallery_posts` | ממצאי גימטריה שהועלו דרך הטופס. |
| `chat_messages` | הודעות הצ'אט הקהילתי (realtime). |
| `contact_messages` | פניות מטופס יצירת קשר. |
| `subscribers` | רשימת מנויי דיוור. |
| `legacy_traffic` | סטטיסטיקות גלישה היסטוריות מ‑Jetpack. |
| `nodes` | אירועים/ציר זמן (label, weight, נושא ציר). |
| `els_settings` | הגדרות כלי ה‑ELS. |
| `edges` / `discoveries` / `entities` / `bidim` / `calculator_anchors` | טבלאות שדה ל‑realtime. |

---

## 6. תבנית לרשומה חדשה

מכאן והלאה — להוסיף רשומה לכל יום עבודה בראש סעיף "יומן כרונולוגי" או בהמשך מסמך זה:

```
## YYYY-MM-DD — <כותרת קצרה>
- מה נעשה:
- קבצים שהשתנו:
- הערות / להמשך:
```

---

## 2026-06-11 — הכנות לפני העברת הדומיין (cutover)

**מה נעשה:**
- **ניתוק מ-WordPress (הקפאה מלאה):** כיבוי הסנכרון היומי האוטומטי (`sync-posts.yml` → הרצה ידנית בלבד),
  הסרת קריאת תפריט WordPress (`fetchWpMenu` — התפריט סטטי), ומחיקת `api/wp-posts.js`.
- **SEO/UX:** רכיב דף 404 אמיתי (`NotFound`) + `noindex` לסלאגים לא-מוכרים; `schema.org/Article` בדפי פוסט;
  הפניות 301 היגייניות ב-`vercel.json` לנקודות-קצה וורדפרס שבוטלו (`/feed`, `/wp-admin`, `/wp-login.php`, `/xmlrpc.php`).
- **אבטחה:** סיסמאות האדמין והדוח עברו ממחרוזת מקודדת למשתני סביבה (`VITE_ADMIN_PASSWORD`, `VITE_REPORT_PASSWORD`).
- **OCR גלריה:** עיבוד כל תמונות הגלריה דרך ה-Edge Function `gallery-ocr` הושלם (זיהוי מספרים/גימטריה/תאריכים).
- **מדיה (תמונות בתוכן הפוסטים):** התברר שהמדיה כבר הועברה ל-Supabase storage (מיגרציית FTP מיותרת).
  שני סקריפטים חדשים החליפו כתובות `wp-content` בכתובות storage, **רק לקבצים שאומתו כקיימים** (בלי נסיגה):
  - `scripts/rewrite-content-urls.mjs` — מדיה באנגלית: **174 קבצים → 93 פוסטים**.
  - `scripts/rewrite-hebrew-media.mjs` — מדיה בעברית (שחזור התעתוק עברית→אנגלית + אימות): **401 קבצים → 177 פוסטים**.
  - תוצאה: **~94% מהפניות המדיה מצביעות עכשיו ל-storage** (18,730 מול 1,106).
  ⚠️ **נותר:** 217 קבצים ייחודיים (185 עברית + 32 אנגלית) **שמעולם לא הועלו ל-storage** — בעיקר וידאו/אודיו
  גדולים, PDFים ותמונות בודדות (`mp4:31 pdf:29 mp3/wav:11 jpg/png:138`). יישברו ב-cutover אם לא יועלו מחדש.

**קבצים שהשתנו:** `src/App.jsx`, `src/lib/seo.js`, `vercel.json`, `.github/workflows/sync-posts.yml`,
`migrate-media.mjs`, `scripts/rewrite-content-urls.mjs`, `scripts/gen-sitemap.mjs`, `public/robots.txt`, `public/sitemap.xml`.

**הערות / להמשך — דרוש הגדרה ממך לפני ה-cutover:**
1. **Vercel → Environment Variables:** `VITE_ADMIN_PASSWORD`, `VITE_REPORT_PASSWORD` (אחרת `/admin` ו-`/traffic` נעולים).
2. **217 קבצי מדיה חסרים** — לא קיימים ב-storage (185 עברית + 32 אנגלית; וידאו/אודיו/PDF גדולים בעיקר).
   רוב המדיה כבר מופתה (94%). את ה-217 צריך להעלות מחדש מהשרת הישן (FTP) לפני ה-cutover, או להשלים עם אובדנם.
3. **הרשאות RLS** לטבלאות ב-Supabase (E בצ'קליסט).
4. **תמונת שיתוף** `og:image` בגודל 1200×630 (D.8).
5. **Vercel → Domains:** הוספת `sod1820.co.il` + `www` (F).

# פוסטי תפילה + סגולה לזיווג — מהינוקא

תיעוד פרסום (הפוסטים עצמם חיים בטבלת `posts` ב-Supabase, פרודקשן `linswmnnkjxvweumprav`).

## רקע / מקור
- סגולת הרב **שלמה יהודה ("הינוקא")** שליט"א לזיווג: אמירת פרשת **קרבנות הנשיאים** (במדבר ז') בכל יום ויום (כולל שבת), מיד לאחר תפילת שחרית, עד שנושעים בזיווג.
- מקורות: `tehillim-center.co.il/sgula/709` (נוסח הגבר), `hayanuka.com`.
- כרטיס התפילה לאישה (התמונה ששלח צוריאל) הועלה ל-Storage:
  `gallery/prayer/2026/06/tefila-zivug-isha-hayanuka.jpg`.

## הפוסטים שנוצרו
| שדה | פוסט גבר | פוסט אישה |
|---|---|---|
| id | 2602 | 2603 |
| wp_id | 36937 | 36938 |
| slug | `תפילה-וסגולה-לזיווג-לגבר-מהינוקא` | `תפילה-וסגולה-לזיווג-לאישה-מהינוקא` |
| author | מזכה הרבים | מזכה הרבים |
| categories | התחזקות | התחזקות |
| source / space | ai / core | ai / core |
| image_url | תמונת ההינוקא (media bucket, שימוש חוזר) | כרטיס התפילה לאישה (gallery bucket) |

תגיות: `הינוקא`, `תפילה`, `סגולה`, `זיווג`, `שידוך`, `קרבנות הנשיאים` (+ `מעוכבת שידוך` בפוסט האישה).
כל התגיות הקיימות נוצלו (לא נוצרו כפילויות; `הינוקא` ולא `הינוקה`, `תפילה` ולא `תפילות`).

## SEO / שיתופים
- `excerpt` ממוקד מילות-מפתח (משמש כ-`og:description` / `meta description`).
- `image_url` משמש כ-`og:image` ו-`twitter:image` (כרטיס שיתוף).
- כותרות עשירות + מבנה `<h2>` + קישורי «ראו גם» הדדיים בין שני הפוסטים ולפוסט "תפילה לרפואה של ההינוקא".
- שורת קריאה לשיתוף/זיכוי הרבים בתחתית כל פוסט.

## חוטים פתוחים
- אם תישלח תמונה ייעודית/מעוצבת לפוסט האישה — להחליף `image_url`.
- ערכי גימטריה (אם יתבקשו) — רק דרך המנוע הרשמי (`src/lib/gematria.js` / `fn_*`).

# 2029 Number Prime Lens + 787→138→צמח + Beit Midrash Learning Plan v1

**Status:** HUMAN-GATE ACCEPTED PLAN · DOCS-ONLY · NOT PRODUCT IMPLEMENTATION  
**Human Gate:** ZURIEL · 2026-09-22  
**Owner check:** EXTEND_EXISTING only. No Prime Engine, no new Number store, no new truth system, no new graph.  
**Canonical owners:** research_strategy_layer_law v15 · reality_graph_law v8 · truth_axes_foundation_law v3 · canonical_methods_registry_law v6 · existing number_math_profile capability.

## Decision

ZURIEL accepted the Prime Lens recommendations and requested that they enter the 2029 program.

The implementation direction is to extend the existing deterministic `number_math_profile` and its existing Number/Research projections. Prime-number research is a mathematical lens over a Number identity, not a separate engine.

## Existing live foundation

The released `number_math_profile` already provides deterministic primality, bounded factorization, divisor/totient properties, semiprime, polygonal families, powers, decimal palindrome, repdigit, Harshad, happy, Armstrong and derived palindromic-prime classification.

The future work in this plan is additive context, relation discovery, prioritization and explanation around that existing output.

## Prime Lens — target capability

For a Number N, expose a bounded and replayable Prime Context when applicable:

- primality / composite classification;
- prime index when N is prime;
- previous and next prime;
- prime gaps around N;
- special prime-family predicates where mathematically defined and useful, including palindromic prime, superprime, twin/cousin/sexy relations, safe prime, Sophie Germain prime, emirp, circular prime and truncatable prime;
- bounded prime-sum decompositions;
- bounded consecutive-prime-sum detection;
- exact provenance for every deterministic result;
- explicit incomplete/bounded state rather than fabricated certainty.

Not every family belongs in the first UI projection. Capability may calculate more than the glance layer displays.

## Dependency honesty / ranking

Derived labels must not inflate evidence.

Example:

`palindromic_prime = prime + palindrome_base10`

It is useful as a human-facing mathematical family, but it is dependent on those two underlying facts and must not count as a third independent convergence.

The Number 2029 glance layer should rank mathematical notability rather than display the first array entries mechanically. A derived/high-information family such as **ראשוני פלינדרומי** may outrank generic `prime`, `palindrome` and `deficient`, while preserving the dependency relation underneath.

## Golden fixture — 787 → #138 → צמח

This chain is approved as a Golden research fixture because it demonstrates how mathematical structure should reconnect to the existing Number/Gematria tree without hard-coded special cases.

Deterministic chain:

1. `787` is prime.
2. Its prime index is `138`.
3. Canonical Gematria Registry execution for regular Gematria verifies:
   `fn_ragil('צמח') = 138`.
4. Therefore the system may project the navigable chain:
   `787 → prime index #138 → Number 138 → צמח = 138 (רגיל)`.

Truth boundary:

- `787 is prime` = mathematical engine fact.
- `prime_index(787)=138` = mathematical engine fact.
- `צמח=138` in method `רגיל` = canonical Gematria calculation fact.
- any statement that this gives a deeper symbolic meaning or connects 787 to a broader “צמח system” = research interpretation/convergence candidate, not mathematical fact.

### One-tree requirement

Do **not** hard-code `787 → צמח`.

The generic behavior is:

`mathematical relation produces Number M → resolve Number M through existing Number/Reality/Gematria graph → surface relevant verified Expressions/Topics/relations`.

Thus any future number whose prime index, factor, divisor, sequence position or other governed mathematical relation produces `138` can discover the same Number-138 world through the same tree.

## 138 curation state

Live `number_anchors` currently has no row for 138.

ZURIEL identifies 138 as one of his strong/recurrent numbers, but did not yet decide that it should become a canonical Number Anchor.

Plan state:

- preserve 138 as a **Human-Gate curated strong-number / anchor candidate** in planning/research context;
- do not insert it into `number_anchors` merely from this conversation;
- evaluate anchor promotion later against the active anchor owner/criteria and broader recurrence evidence;
- anchor status must not be inferred from personal importance alone.

## Number 2029 projection work

Future isolated product pass should:

1. add Prime Context to the existing mathematical section rather than a new page/system;
2. show prime index, neighbors/gaps and selected special families in a compact hierarchy;
3. support a “why?”/deep view that explains the deterministic derivation;
4. resolve resulting numbers back into the Number/Gematria/Reality tree;
5. use 787 as a Golden acceptance fixture for the `#138 → צמח` bridge;
6. preserve Rank-Don’t-Hide and dependency grouping;
7. never auto-promote a mathematical relation into a convergence/claim/canonical interpretation.

Suggested 787 glance:

- ראשוני פלינדרומי
- ראשוני · #138
- previous/next prime + gaps
- “#138 opens Number 138” → verified expressions, including `צמח = 138`
- deeper bounded structures such as consecutive-prime sums in the expanded view.

## Prime Scan — research procedure, not engine

Preserve the existing Research Intake decision:

`corpus/tokenization → canonical Gematria calculation per expression → deterministic primality predicate → observations/results`.

Prime Scan is a Procedure composed from existing primitives. It must not become a Prime Engine, Tokenizer Engine or second research pipeline.

Future uses may include:

- scan a Book/source/post/approved corpus for expressions whose verified Gematria values are prime;
- filter for special prime families;
- compare prime-index relations;
- return ordinary Research Objects/Result Bundles with source and calculation provenance.

## Heichal — Prime Research Lane

The Heichal is the deep execution surface for Prime/number-pattern research. It does not own prime truth and it does not become a separate Prime Lab.

Product-role separation:

- **Number / Expression** — identity/profile of the active number; compact mathematical passport and selected high-information Prime Lens facts.
- **World** — breadth, relations, neighboring entities/topics and contextual connections around the active number/root.
- **Heichal** — deep research execution over the same Research Context, with bounded procedures, Evidence/Findings and Explain-Why/Trace.
- **Beit Midrash** — learning/explanation of mathematical and Gematria concepts; it does not execute a second computational engine.

### Heichal entry / continuity

A Number/Expression may enter Heichal with the existing Research Context intact. Prime research must preserve:

`subject/root → focused mathematical relation → method/procedure inputs → result → exact return`.

For the Golden fixture:

`Number 787 → Heichal → inspect prime index → 138 → resolve Number 138 → canonical Gematria relation צמח=138`.

The Heichal must never hard-code `787→צמח`; it traverses the generic Number/Reality tree.

### Prime research actions

Contextual actions may include, when applicable and bounded:

- inspect primality proof/classification and prime index;
- previous/next prime and prime gaps;
- inspect special prime families and their exact predicate;
- bounded prime-sum decompositions;
- bounded consecutive-prime-sum search;
- compare prime structures across selected Numbers;
- Prime Scan over an authorized source/corpus using the existing Procedure contract;
- resolve any numeric result back into the existing Number/Gematria/Reality graph;
- ask Raziel to explain results or propose a next research step without changing deterministic truth.

These actions belong naturally under the existing Pattern / Sequence workbench capability family in Heichal. They are not separate applications.

### Result / evidence boundary

A Heichal Prime action returns deterministic calculation/search results into the existing Research Result/Universal Finding path with provenance and bounded-state metadata.

Required distinctions:

- mathematical result != Gematria result;
- either result != Finding/Claim automatically;
- relation discovered through traversal != independent evidence if it derives from the same underlying operands;
- AI explanation != calculation;
- Human Gate remains required for governed canonicalization/publication.

### Dependency gate before broad Heichal build

Do not build a large bespoke Prime UI before the Roadmap foundation sequence closes enough of:

`Operational Trace → server capability/entitlement/budget → Experience Context → Research Plan/Path resumability`.

Prime Lens is an early Golden consumer after those seams because it is a clean test of Number→Heichal→Number/World continuity, deterministic Trace, bounded procedures and exact return.

## Beit Midrash — learning/explanation requirement

Add a learning section in the Gematria area of Beit Midrash that explains the mathematical structure behind Number pages in plain Hebrew.

Working title:

**מספרים ראשוניים וה-DNA המתמטי של מספר**

Minimum learning scope:

- what a prime number is;
- prime vs composite;
- factorization and why primes are the “building blocks” of integers;
- prime index;
- previous/next prime and prime gaps;
- palindromic prime;
- superprime and the distinction between “prime index” and “index is also prime”;
- semiprime;
- consecutive-prime sums;
- examples of correct/incorrect claims and how the system verifies them;
- clear separation between mathematical fact, Gematria calculation, research relation and interpretation;
- links into live Number pages / examples, with 787 as a primary teaching example.

Content should consume/describe the existing mathematical capability; it must not create a second definition registry.

## Documentation drift to close

`docs/g3-number-math-profile-adapter-v1.md` still says BRANCH-ONLY / NOT RELEASE-AUTHORIZED even though the capability was merged/released via PR #490 and is consumed by Number 2029.

Future docs-maintenance pass should reconcile that status against live main. Do not treat the stale header as current state.

## Roadmap navigation pointer

The Master Roadmap should receive one compact navigation bullet, not the detail above:

> **Number Mathematical/Prime Lens:** extend the released `number_math_profile` with prime context/relations and one-tree numeric re-entry; Golden fixture 787→prime index 138→Number 138→verified `צמח=138`; add Beit Midrash prime-number learning explanation. 138 remains a curated anchor candidate until a separate Human-Gate anchor decision. Detailed plan: `docs/2029-number-prime-lens-beit-midrash-plan-v1.md`.

This pointer is **pending** because open Draft PR #556 currently owns `SOD1820_MASTER_ROADMAP.md`. Do not create a parallel writer or modify the Roadmap until that scope clears.

## Implementation order

1. clear Roadmap writer conflict and add the compact pointer;
2. reconcile stale Number Math Profile documentation status;
3. extend deterministic Prime Context / relation helpers under the existing capability;
4. add focused tests including 787 / 138 / false-superprime checks;
5. integrate Number 2029 projection in an isolated UI pass;
6. add Beit Midrash learning section through the existing Gematria/learning surface;
7. visual + truth-boundary acceptance;
8. only later decide whether 138 merits canonical Number Anchor status.

## Acceptance

The plan is satisfied when:

- no Prime Engine or parallel store exists;
- 787’s prime index is derived deterministically as 138;
- `צמח=138` is obtained through the canonical Gematria engine, not hard-coded arithmetic;
- the relation is traversable through Number/Reality identity rather than a one-off link;
- 138 anchor status remains honest and separate from curated importance;
- Prime Lens derived families do not inflate independent evidence;
- Beit Midrash explains the subject clearly to a non-expert;
- mathematical fact, Gematria fact and interpretation remain visibly separate.

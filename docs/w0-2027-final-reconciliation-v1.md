# SOD1820 — W0 2027 FINAL RECONCILIATION V1

**Status:** POST-CHALLENGE RECONCILIATION · BRANCH ONLY · W0 STILL ACTIVE · NO PRODUCT IMPLEMENTATION  
**Human Gate:** ZURIEL  
**Independent challenge:** work_log `d3c26a5c-1a01-47b9-9147-a94430c65f3c`  
**Owner verdict:** `EXTEND_EXISTING`

## 0. Purpose

This document reconciles the independent Claude challenge with ZURIEL's decisions and the 2027 planning branch. It is not a new owner, contract, graph, store, engine, registry or release artifact.

The 2027 direction remains approved as planning direction:

**One Reality Graph · One Research OS · One Adaptive Research Experience · One World, Many Views.**

Claude verdict: **PASS WITH CONDITIONS**. No genuinely new global owner is required.

---

## 1. Human-Gate decisions after the challenge

### HG-1 — Number / Phrase

**Decision:** the current Number/Entity presentation, including the visual form of the old findings area, may be rebuilt from first principles later.

What is preserved is the capability contract, not the old box/layout:

`Discovery Engine → Number/Phrase identity → findings projection`

The Discovery Engine output target must be re-homed into the future Number/Phrase renderer; it must not disappear when the old S10.4 presentation is superseded.

**Sequencing decision:** do not redesign the full Number/Phrase experience now. First connect the existing Number/Phrase experience into the 2027 system; perform the deep redesign only after World, Journeys, Raziel, Books, ELS, My Workspace and the new Shell are visible enough to make the final composition decision with evidence.

### Number/Phrase signature capabilities to preserve during the bridge

These are explicitly protected as product capabilities while presentation remains redesignable:

- Number/Phrase stable identity and direct SEO/deep-link entry;
- vitality / pulse — how alive the number is in the system, with explainable real inputs; HOT/ACTIVE != TRUE;
- convergence snapshot/map — the large high-value convergence view;
- method switching as real research state;
- downstream dynamic update when active method changes;
- cross-method equality/intersections, including hidden intersections where governed;
- existing AI number analysis capability, re-presented as Raziel-aware interpretation rather than a separate detached AI identity;
- Discovery Engine findings/output;
- sources, relations, Books, ELS, Posts/Galleries/Events and World connections;
- add/save/research continuity and `return_exact`.

Bridge principle:

**CONNECT NOW · REDESIGN LATER.**

The current Number/Phrase core may remain visually recognizable during W1–W7 while adapters are added around it. Temporary additive modules below/around it are allowed. Do not force a premature rewrite merely to make the 2027 Shell look complete.

---

### HG-2 — Legacy Command Center / Admin

**Decision:** the legacy CC-1..CC-4 Admin/Command-Center UI program is **ABSORBED INTO 2027**, not maintained as a parallel future UI program.

Presentation/phasing specific to the old Command Center does not control the future 2027 Admin experience once the owner reconciliation is applied.

Must preserve underlying capabilities and boundaries where still valid:

- Human Gate;
- truth/governance/provenance;
- candidates/review;
- source intake;
- safe write boundaries;
- analytics/traffic/system operations;
- Metatron/system intelligence;
- identity/security/authorization;
- unique useful read projections.

Target distinction remains:

- **Admin Research / Human Gate Workspace** — research, sources, candidates, review, relation composition.
- **System Operations / Metatron** — system health, analytics, diagnostics, operational intelligence.

They may share one Admin shell; they are not the same semantic domain.

The Canonical Owner Index requires an additive Admin/Command-Center routing row during canonical reconciliation; no new owner is implied.

---

### HG-3 — Roadmap v5.3 carry-forward

**Decision:** v5.4 must carry forward still-open obligations/gates from v5.3 before it is released as the navigation SSOT.

The byte-identical v5.3 archive preserves provenance but is not enough for active navigation.

Required carry-forward form:

`old workstream/gate → existing owner → live status/evidence → 2027 W-stage or separate retained track → next action / Human Gate`

Especially preserve any still-open `DO NOT BUILD YET` / Human-Gate condition so a future agent cannot cross it merely because v5.4 omitted the row.

Carry-forward does not revive legacy UI or priority by default. Every old item must be classified as one of:

- CLOSED / already satisfied;
- ABSORBED into W0–W9;
- RETAINED separate track;
- PARKED;
- SUPERSEDED by explicit ZURIEL decision;
- OPEN HUMAN GATE.

---

## 2. Claude challenge findings adopted

### Adopted drift set

W0 owner reconciliation must account for all six identified areas:

1. `docs/sod1820-system-frame-contract-v1.md` — fixed Top Navigator / Context Rail / One Dock presentation.
2. `docs/research-studio-v1-contract.md` — fixed three-column Workspace language and Number `never rebuilt/replaced` presentation wording.
3. `research_workspace_law` — fixed Shell/workspace presentation.
4. `workspace_layout_standard` — locked three-column form and no-fullscreen-tool rule.
5. legacy Command Center / CC chain in Master State — absorbed by HG-2 while preserving valid capabilities/boundaries.
6. Master State S10.4 Number findings area — visual lock superseded by HG-1; Discovery Engine output-target capability preserved and re-homed.

No new global owner is needed.

### False blockers retired

- exhaustive route ledger is a finite/mechanical W0 deliverable, not an architecture blocker;
- no W0↔Golden-Slice circular gate is introduced. W0 is specification/owner closure; executable Golden proof belongs to later implementation acceptance.

---

## 3. Routing / SEO findings adopted

Current routing has two legacy hazards that W0/W1 must explicitly handle before route retirement:

- root-level `/:slug` posts can collide with future top-level product routes;
- unmatched multi-segment routes currently fall through to `* → /`, creating a silent redirect to Home.

2027 requires an **honest terminal projection** using existing routing/access owners:

- not found;
- moved/retired with deliberate redirect where valid;
- access gated / upgrade required;
- unavailable/changed source/result;
- no permission;
- invalid/stale exact-return target.

Never silently replace a missing/deep target with unrelated Home content.

Identity addressability must also reconcile declared `entity_types.route_pattern` values with actual registered routes before generic World links assume direct addressability.

---

## 4. Language + access / Premium readiness

Claude independently confirmed the structural model.

Language:

- one identity/state across locale;
- locale is representation projection;
- global `change_language` capability;
- RTL/LTR-safe Shell from foundation;
- Research Context/Journey/orientation survive locale changes;
- Raziel uses the same authorized context and may converse in a different language from the displayed source where supported.

Access:

- capability availability != caller entitlement != object publication/access != truth;
- Public / registered / Premium / Deep / Admin/private are projections over the same identities;
- server bounds payload before UI rendering;
- no duplicate Premium Number/Book/Post/World identities/pages;
- Raziel cannot receive hidden material the caller is unauthorized to retrieve.

Non-claim: billing/subscription runtime and localization execution are later implementation work.

---

## 5. W0 finite closure set after challenge

The independent challenge is now complete. W0 remains active until the following specification/owner items are closed:

1. six-area owner-reconciliation delta from §2;
2. exact route/alias/SEO ledger, including post-slug collision policy and honest terminal projection;
3. identity addressability matrix;
4. personal capability owner matrix, preserving the `personal_command_center_law` next-action product intent without its legacy presentation;
5. content publication vs Research Intake matrix;
6. cross-cutting capability map: Follow / Share / Language / Onboarding / Legal-Privacy / Install-Push;
7. access / privacy / ownership / entitlement / operational-role / Human-Gate authority matrix;
8. action-execution and cost/credit/retry/idempotency semantics;
9. exact-reopen adapter/state-reference map;
10. semantic telemetry + first-surface performance budgets;
11. v5.3 open-workstream/gate carry-forward table;
12. Number/Phrase bridge contract from HG-1.

W0 is a specification/owner gate. Do not claim executable proof until the relevant W1/W2/later code exists.

---

## 6. Build sequencing clarification

Recommended experience sequence after W0 owner/spec closure:

1. **Visual Foundation 2027** — reconcile Design Contract/tokens before broad UI construction.
2. **W1 Adaptive Shell + Research Context** — orientation, navigation projection, commands, Raziel entry, My Workspace entry, language/access-ready structure, honest terminal states.
3. **W2 World Golden composition** — bounded real entity/world composition.
4. **W3 Heichal/tool continuity**.
5. **W4 Books**.
6. **W5/W6 new Admin Research + System Operations composition**, absorbing legacy CC presentation per HG-2.
7. **W7 Raziel deeper orchestration**.
8. **W8 Premium/Deep projections** after security/entitlement gates.
9. **W9 multi-surface continuity**.
10. **Number/Phrase final redesign** only after enough of the above experience is visible to make the final product composition decision. It may occur earlier if ZURIEL explicitly decides sufficient evidence exists, but is not required to unblock the Shell/World program.

Throughout W1–W9, Number/Phrase remains a first-class directly addressable product and is progressively connected to the new system.

---

## 7. Release state

This reconciliation records planning decisions only.

`DOCUMENTED ON BRANCH != CANONICAL OWNER RECONCILED != MERGED != DEPLOYED != LIVE != VERIFIED`

No product code, schema, active DB rule, canonical truth, merge or deployment is authorized by this artifact alone.

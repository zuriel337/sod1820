# 🔬 סביבת-העבודה למחקר — Audit + Design (READ-ONLY · actor=CLAUDE · 14.8.2026)

> **מעמד:** דוח ניתוח-ותכנון בלבד. ❌ לא נבנה כלום · ❌ אין migration · ❌ אין שינוי DB · ❌ אין שינוי Master State · ❌ אין פריסה · ❌ אין UI implementation.
> **עיקרון-על שהנחה את כל הדוח:** *אם אפשר לפתור על-ידי חיבור נכון של משהו שכבר קיים — לא מציעים לבנות חדש.* לכן עמודת **C (reuse)** היא הגדולה, ו-**D (WRITE אמיתי)** מצומצם למינימום.

---

## 📋 מקרא — מילון-הסטטוסים (סעיף 1, נאכף בכל הדוח)
`✅ בוצע` · `⚠️ דורש אימות` · `🔴 חסר מידע` · `⚪ לא נבדק` · `◻️ לא רלוונטי`.
«לא נגיש» **אינו** סטטוס — הוא מצב-הרשאה. כשמשהו חסר, מציינים **מה** חסר: `תאריך` / `מקור` / `ערך` / `אימות-מנוע` / `מידע-מהכותב`.

---

# חלק א׳ — טבלת A–H לפי 15 הנושאים

לכל נושא: **A** מה קיים · **B** מה חסר · **C** reuse · **D** WRITE אמיתי · **E** Human-Gate · **F** יכול להישאר Read-only · **G** סכנת-מקבילה · **H** סדר.

---

## 1 · מילון-סטטוסים אחיד
- **A (קיים):** `ccwork.js` כבר מחזיק את הפרימיטיבים — `ACT_STATE={done,pending,blocked,missing}`, `actionState(it,action)`, `normStatus(it)`, `whatMissing(it)` (מחזיר את הפעולה-החסרה הראשונה = «הצעד הבא»). WarRoomTab כבר מרנדר «מצב-פעולות» per-action.
- **B (חסר):** אין אייקון/תווית אחידים ל-5 המצבים על-פני כל המשטחים; `whatMissing` מחזיר *פעולה* ולא *סוג-המידע-החסר* (תאריך/מקור/ערך/אימות/כותב).
- **C (reuse):** להרחיב את `ACT_STATE` ל-5 ערכים + מפת-אייקונים אחת; להרחיב את `whatMissing` שיחזיר `{action, missingKind}`. אותה פונקציה, לא חדשה.
- **D (WRITE):** אין. לוגיקה טהורה בקובץ קיים.
- **E (Human-Gate):** לא — תצוגה בלבד.
- **F (Read-only):** ✅ כולו.
- **G (מקבילה):** גבוהה אם ניצור enum-סטטוס שני. **חובה מקור-אמת יחיד = `ccwork.js`.**
- **H:** ראשון — כל שאר הנושאים נשענים על המילון הזה.

## 2 · «סיום בדיקה» ≠ «אישור קנוני»
- **A:** קיימות שתי שכבות נפרדות: `handled.js` (`markHandled`→`research_items` bucket='handled', סגירת-תור אישית, המקור לא נמחק) מול השרשרת הקנונית `research_items → findings → verified → canonical → published`. סיבות-סגירה קיימות (`טופל/לא-רלוונטי/כפול/נבדק/נותב`).
- **B:** ב-UI שני המושגים מיטשטשים — «סיום בדיקה» ו«אישור» נראים דומה; אין כפתור-אישור-קנוני נפרד ומגודר.
- **C:** «✅ סיום בדיקה» = `markHandled` הקיים (סוגר+מסכם, **לא** מקדם). «✓ אשר ממצא» = צומת-הקידום הקנוני הקיים (research_items→findings/verified).
- **D:** אין טבלה חדשה. לכל-היותר שדה-סטטוס בתוך `research_items.metadata` הקיים.
- **E (Human-Gate):** ✅ **«✓ אשר ממצא» חייב שער-אדם.** «סיום בדיקה» = פעולה אישית, בלי שער.
- **F:** «סיום בדיקה» יכול להיות Read-mostly (כותב רק לתור-האישי). «אישור» — WRITE מגודר.
- **G:** סכנה גבוהה — אם «סיום» יקדם בטעות לקנוני. ההפרדה היא-היא ההגנה.
- **H:** שני — מיד אחרי המילון; זה עמוד-השדרה של ה-Governance.

## 3 · עריכת טקסט-המקור (RAW נשמר)
- **A:** `research_items`/מקורות מחזיקים טקסט-מקור; provenance נשמר. RAW לא משתנה בשום זרימה קיימת.
- **B:** אין «נוסח-עבודה» נפרד הניתן-לעריכה לצד ה-RAW; אין revert-ל-RAW; אין חיווי «איזה טקסט הוזן לניתוח».
- **C:** לאחסן «נוסח-עבודה» כשדה בתוך `metadata` של הרשומה הקיימת (`work_text`), עם `raw_text` שמור-לצמיתות לצדו. אותו record.
- **D (WRITE):** ✅ **כן — כאן יש WRITE אמיתי** (שמירת נוסח-עבודה). אבל בתוך שדה-metadata קיים, לא טבלה חדשה. RAW = לקריאה-בלבד לעד.
- **E:** עריכת נוסח-עבודה = פעולת-משתמש; **החלפת RAW = אסורה** (לא שער — פשוט חסום).
- **F:** ניתוח-על-RAW נשאר תמיד אפשרי (Read-only). ניתוח-על-נוסח-עבודה = עתידי, עם חיווי-מקור מפורש.
- **G:** סכנה קריטית — אם «עריכה» תדרוס RAW. הכלל: `🔒 מקור RAW` + `✏️ נוסח עבודה` שני-שדות, לעולם לא אחד-דורס-שני.
- **H:** מאוחר (אחרי 1,2,4,5) — נדרש רק כשמתחילים לנסח.

## 4 · Progressive Disclosure
- **A:** התבנית קיימת באתר (`InsightCard` פתיחה-בהדרגה, `research_workspace_law` «פשוט→חזק»).
- **B:** בתוך Command Center הניתוח-המלא נחשף בבת-אחת; אין ברירת-מחדל פשוטה «🔬 ממצא + הצג-מסלול-מלא ▾».
- **C:** אותו דפוס collapse שכבר מומש ב-«נכנס עכשיו» (state+localStorage ב-WarRoomTab) — להחיל על גוש-הניתוח.
- **D:** אין (state מקומי + localStorage).
- **E:** לא.
- **F:** ✅ כולו.
- **G:** נמוכה.
- **H:** אחרי 1,6 (צריך את התצוגה-האחידה קודם).

## 5 · «🎯 הצעד הבא» per-item
- **A:** `whatMissing(it)` **כבר** מחזיר את הצעד-החסר הראשון = הפרימיטיב לצעד-הבא. WarRoomTab כבר מציג מצב-פעולות.
- **B:** אין תווית-המלצה בולטת אחת «🎯 הצעד הבא» שמתרגמת את `whatMissing` למשפט-פעולה.
- **C:** עטיפה דקה מעל `whatMissing` — לא לוגיקה חדשה.
- **D:** אין.
- **E:** לא — **המלצת-workflow, לא קנוניזציה.** חייב להיאמר במפורש «המלצה» ולא «אמת».
- **F:** ✅.
- **G:** נמוכה, כל עוד זה «המלצה» ולא פעולה-אוטומטית.
- **H:** אחרי 1.

## 6 · תצוגת שרשרת-הממצא אחידה + סטטוס-לכל-שכבה
- **A:** `GematriaFinding` + `bidim` (`phrase×method×value`) + `convergences` נושאים את השרשרת. הסטטוסים מ-#1 מכסים כל שכבה.
- **B:** אין רכיב-תצוגה אחד שמראה את השרשרת המלאה (מקור→חילוץ→ביטוי→שיטה→ערך→הצלבה→התכנסות→מספר→גרף→מקור) עם סטטוס-per-שכבה.
- **C:** להרכיב מ-`GematriaFinding` הקיים + מילון-הסטטוסים; רכיב-תצוגה יחיד (כמו `InsightCard`), לא מנוע.
- **D:** אין (תצוגה בלבד).
- **E:** לא.
- **F:** ✅.
- **G:** גבוהה — סכנה לבנות «מנוע-שרשרת» חדש. זו **עדשה** על הנתונים הקיימים בלבד.
- **H:** אחרי 1; בסיס ל-3,7,8.

## 7 · חוק-גימטריה גלובלי — ביטוי × שיטה × ערך + drill-down מילוי
- **A:** החוזה **כבר קיים כנתון:** view `bidim` = `(word_id, phrase, method, value, is_verified)`. `gematria_words` = עמודה-לכל-שיטה. `fn_gematria_pack(phrase)` = חבילת-מחקר דטרמיניסטית (`used_llm:false, tokens:0`). `fn_miluy_letter(letter)` = מקור-drill-down למילוי per-אות.
- **B (הלב):** האובדן **בתצוגה** — הכל ננעל על `ragil`: `getEntityBundle .eq('ragil',value)` (supabase.js:1794), `GEM_METHOD_COL={ragil,misratar,kadmi}` (:504, מילוי לא נבחר), `getAllValuePhrases .eq('ragil')` (:3093). לכן `ובתורתו=1020·רגיל` ו-`ובתורתו=1820·מילוי` לא מתחברים בתצוגה.
- **C:** להחליף את מפתח-התצוגה מ-`ragil` ל-`bidim(phrase,method,value)` בפונקציות-הקריאה הקיימות. הנתון כבר שם — רק לקרוא נכון.
- **D (WRITE):** אין WRITE ל-DB. שינוי הוא ב**קוד-קריאה** בלבד (וזה שינוי-קוד, לא נושא-האודיט-הזה — כאן רק ממפים).
- **E:** לא.
- **F:** ✅ (קריאה בלבד; הנתון קיים ומאומת).
- **G:** קריטית — **אסור** מנוע-גימטריה מקביל או טבלת-ערכים חדשה. `bidim`+`fn_gematria_pack` הם המקור היחיד.
- **H:** מוקדם-אמצע — בסיס ל-6,8 ולחוק-הניווט.

## 8 · 🌉 גשרים כמנגנון-אחד (מילוי/אנגלית/אנגרמות/ELS/ציר/מקורות/כותבים)
- **A:** `GematriaFinding.bridges[]` קיים כמבנה. חוזה-הניווט כבר נשמר ב-`convergence_navigation_contract.md` (טיוטה).
- **B:** אין מימוש-ניווט של גשר; אין קריטריון «קשר משמעותי» / סף «צומת משמעותי» (צוריאל טרם קבע — אסור להמציא).
- **C:** מנגנון-אחד `צומת → קשר → צומת → מקור` מעל `bridges[]` הקיים; לכל סוגי-הקשר אותו מנגנון.
- **D:** אין כרגע (החוזה תכנוני; הקריטריונים בהמתנה להחלטת-צוריאל).
- **E:** קביעת-סף «צומת משמעותי»/«קשר משמעותי» = **החלטת-צוריאל** (לא Human-Gate טכני — החלטה-אנושית עקרונית).
- **F:** ✅ בשלב-התכנון.
- **G:** קריטית — «אל תבנה כ-Gematria-בלבד», ואל תבנה מנגנון-נפרד-לכל-סוג-קשר.
- **H:** אחרי 6,7; דורש קריטריונים מצוריאל לפני מימוש.

## 9 · 📖 מקור-מלא חובה per סוג-מקור
- **A:** `GematriaFinding.source.url` נושא `post`/`comment`/`wa`/`ocr`/`contribution`. handled שומר provenance.
- **B:** אין כפתור `🔗 מקור מלא` אחיד שפותח את המקור לפי-סוגו בכל תצוגת-ממצא.
- **C:** רכיב-קישור אחד שקורא `source.kind`+`source.url` הקיימים ומנתב.
- **D:** אין.
- **E:** לא.
- **F:** ✅.
- **G:** נמוכה.
- **H:** יחד עם 6 (חלק מרכיב-השרשרת).

## 10 · Header דביק — תיקון ברמת-הארכיטקטורה (גובה-header מרכזי אחד)
- **A:** header יחיד דביק = `Navbar` (`position:sticky; top:0; zIndex:100; height:64` — Navbar.jsx:623/629). Layout.jsx מסדר את השכבות. WarRoomTab/AdminPage **אין** להם sub-header דביק כרגע.
- **B:** אין **טוקן-גובה-מרכזי** (`--header-h`). הגובה 64 קשיח ב-Navbar; רצועות מעל-ה-main (`EnglishSoonBar`/`CelestialPinnedBar`/`CipherElulBanner`) **אינן** דביקות; כל אלמנט-דביק-פנימי עתידי אין לו מקור-אמת ל-offset וגם `scrollMarginTop` לעוגנים אין לו טוקן.
- **C:** להגדיר **פעם אחת** `--header-h:64px` על ה-root/Navbar; כל sticky-offset ו-`scroll-margin-top` יקראו את המשתנה. זה ה«תיקון ברמת-הארכיטקטורה» שהתבקש.
- **D (WRITE):** ✅ **כן — שינוי-קוד קטן ומרכזי אחד** (הגדרת CSS-var + החלפת מספרים-קשיחים בה). לא טבלה, לא DB.
- **E:** לא.
- **F:** ◻️ — זה תיקון-קוד, לא Read-only, אבל מינימלי.
- **G:** נמוכה, כל עוד זו **הגדרה-יחידה** ולא header-חדש. סכנה רק אם ייבנה header שני.
- **H:** עצמאי — אפשר מוקדם; לא חוסם אחרים.

## 11 · 📊 סיכום-עבודה בראש-הניתוח
- **A:** כל הנתונים לסיכום קיימים (ספירת-פריטים, מצבי-פעולה מ-`actionState`, מה-נותב/טופל).
- **B:** אין גוש-סיכום-עליון «כמה נכנס · כמה נבדק · כמה ממתין · כמה נותב».
- **C:** אגרגציה טהורה מעל הפריטים הקיימים + מילון-הסטטוסים (#1). רכיב-תצוגה קטן.
- **D:** אין.
- **E:** לא.
- **F:** ✅.
- **G:** נמוכה.
- **H:** אחרי 1.

## 12 · הפרדת FACT/CLAIM/INFERENCE/INTERPRETATION/RECOMMENDATION/DECISION — AI ≠ אמת
- **A:** ההפרדה **כבר קיימת** בזרימה: `analysisFlow.js` מתייג CLAIM לעומת FACT; `detectArithmetic` שומר `verified` כן/לא; ai-analyze מפריד עובדה-מפרשנות בחוזה. חוקי-ה-Governance (`FACT≠CLAIM≠…`, `HOT≠TRUE`) חקוקים.
- **B:** אין תצוגה אחידה שמציגה את התווית (FACT/CLAIM/…) על כל אלמנט בשרשרת.
- **C:** תגית-סוג אחת בתוך רכיב-השרשרת (#6), נגזרת מ-`analysisFlow` הקיים.
- **D:** אין.
- **E:** עקרון — AI **לעולם לא** מסומן «אמת»; זה אילוץ-תצוגה, לא שער.
- **F:** ✅.
- **G:** קריטית מבחינה עקרונית (לא טכנית) — ערבוב-תוויות = הפרת-Governance.
- **H:** יחד עם 6.

## 13 · חוק-עלות-AI (DB→engine→Smart-Analysis→AI; עובדות-חינם לא מאחורי Sonnet)
- **A:** מתועד ומאושר-כבסיס ב-`ai_cost_open_items.md`. תקרת-Deep=2/יום-לכולם כבר נאכפת ב-`ai_quota_check`. `fn_gematria_pack`/`getWordCrossFacts` דטרמיניסטיים (0 tokens).
- **B:** שני open-items פתוחים: (1) `cross-resonance` עדיין gated מאחורי `aiText` (EntityPage.jsx:860/986); (2) אין cache/dedup ל-«טרי»/«מעמיק» (runAiNumber:867).
- **C:** לשחרר את `aiCross` לטעינת-mount (כמו `getNumberDossier`), ולהוסיף בדיקת-cache לפני `analyzeWordDeep`. שינוי-קוד קטן על קוד קיים.
- **D (WRITE):** אין WRITE ל-DB. שינוי-קוד-קריאה בלבד (open items).
- **E:** לא — להיפך, מסיר-שער מעל עובדות-חינם. Sonnet נשאר opt-in.
- **F:** ✅ (העובדות עצמן קריאה-בלבד).
- **G:** גבוהה — «אסור שעובדה-חינמית תהיה gated מאחורי מנוע-בתשלום».
- **H:** אמצע; open-items כבר ממתינים.

## 14 · תשתית-רזיאל (שכבת-פרשנות מעל העץ — לא DB/גרף חדש)
- **A:** העץ-האחד (`nodes`+`edges`) כבר תומך context מוצלב: `number↕convergence↕person↕date↕event↕source`. `fn_raziel_answer` (deterministic-first) קיים ב-ai-analyze (flag-gated, כבוי). getAiAnalysis(kind='research') קיים.
- **B:** אין הרכבת-context אחת שמזינה את רזיאל מכל צמתי-העץ; רזיאל טרם מופעל למשתמשים.
- **C:** רזיאל = `Analyze(Entity | Collection<Entity>)` מעל `nodes`/`edges` הקיימים + `getAiAnalysis`. **בלי DB/גרף חדש** — שכבת-פרשנות בלבד.
- **D:** אין WRITE חדש; רזיאל קורא את הגרף.
- **E:** ✅ — רזיאל **מציע**, לעולם לא **מחליט**/מקדם-לקנוני. כל קידום עובר Human-Gate (#2).
- **F:** ✅ (קריאת-גרף + פרשנות).
- **G:** קריטית — «רזיאל = שכבת-פרשנות מעל העץ, לא DB/גרף מקביל». זו הסכנה-הכי-גדולה בכל התוכנית.
- **H:** אחרון — נשען על 1,2,6,7,12 (context + סטטוסים + הפרדת-אמת).

## 15 · תוצר = דוח A–H + Roadmap
- הדוח הזה. READ-ONLY, actor=CLAUDE ב-`work_log`.

---

# חלק ב׳ — סיכום-רוחב (איפה WRITE אמיתי, איפה Human-Gate, איפה סכנת-מקבילה)

**D — WRITE אמיתי (רק 3 נקודות, כולן בתוך מבנים קיימים):**
1. #3 — «נוסח-עבודה» בתוך `research_items.metadata` (RAW נשאר לקריאה-בלבד).
2. #10 — טוקן-CSS `--header-h` יחיד (שינוי-קוד מרכזי קטן).
3. #2/#13 — קידום-קנוני ושחרור-cross-resonance = שינוי-**קוד-קריאה**, לא schema/DB.
כל השאר = תצוגה/עדשה מעל נתונים קיימים.

**E — Human-Gate חובה:**
- #2 «✓ אשר ממצא» (קידום לקנוני) · #14 רזיאל-מציע-לא-מחליט · #8 קביעת-סף (החלטת-צוריאל).

**F — יכול להישאר Read-only:** 1,4,5,6,7,9,11,12,13(העובדות),14(קריאה) — רוב-רובו.

**G — סכנת מערכת-מקבילה (סדר-חומרה):**
1. #14 רזיאל → גרף/DB מקביל *(הכי מסוכן)*.
2. #7 מנוע-גימטריה/טבלת-ערכים מקבילה ל-`bidim`.
3. #6 «מנוע-שרשרת» חדש במקום עדשה.
4. #2 «סיום» שמקדם בטעות לקנוני.
5. #3 «עריכה» שדורסת RAW.
**המגן בכל המקרים:** reuse של הפרימיטיב-הקיים + הפרדת-שכבות.

---

# חלק ג׳ — 🗺️ Roadmap מדורג (H)

| שלב | מה | נושאים | סוג | סיכון |
|---|---|---|---|---|
| **0 · יסוד** | מילון-סטטוסים אחיד (`ccwork.js` מורחב) + `whatMissing→missingKind` | 1 | reuse/Read | נמוך |
| **1 · Governance** | הפרדת «סיום בדיקה» (handled) מ«אשר ממצא» (קנוני+Human-Gate) | 2 | reuse + gate | **גבוה** |
| **2 · תצוגה** | רכיב-שרשרת אחיד (עדשה) + סטטוס-per-שכבה + תגית FACT/CLAIM + 🔗 מקור-מלא + 📊 סיכום-עליון + Progressive-Disclosure + 🎯 צעד-הבא | 6,9,11,12,4,5 | reuse/Read | בינוני |
| **3 · גימטריה** | מפתח-תצוגה `bidim(phrase×method×value)` במקום `ragil` + drill-down מילוי | 7 | reuse (קוד-קריאה) | בינוני |
| **4 · עלות** | שחרור cross-resonance מ-gate + cache/dedup ל-AI | 13 | reuse (קוד-קריאה) | נמוך |
| **5 · Layout** | `--header-h` מרכזי אחד | 10 | WRITE קטן | נמוך |
| **6 · ניסוח** | «נוסח-עבודה» לצד RAW (metadata) | 3 | WRITE ממוקד | בינוני |
| **7 · גשרים** | מנגנון-ניווט אחד מעל `bridges[]` — **אחרי קריטריוני-צוריאל** | 8 | reuse + החלטה | **גבוה** |
| **8 · רזיאל** | שכבת-פרשנות מעל `nodes`/`edges` — מציע-לא-מחליט | 14 | reuse + gate | **הכי גבוה** |

**כלל-ברזל לכל שלב:** כל שלב = חיבור-של-קיים ותצוגה; אף שלב לא פותח טבלה/מנוע/גרף חדש. שלבים 1,7,8 דורשים אישור-צוריאל מפורש לפני מימוש. שום שלב לא נפרס בלי «תעלה».

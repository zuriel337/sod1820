# פוסט מתוזמן: «הצופן בקולנוע: האם משחקי הדיונון הם מפת גאולה נסתרת?» 🦑

**סטטוס:** ✅ **מתוזמן לפרסום אוטומטי — ראשון 28.6.2026, 13:00 שעון ישראל** (10:00 UTC).
לא מופיע באתר עד אז. רץ דרך `pg_cron` ב-DB (אמין גם כשהקונטיינר כבוי).

## עיצוב
תיק חקירה (📂) עם הלוגו בראש, בסגנון פוסט מטריקס. שתי תמונות של צוריאל:
- **שער/תמונה ראשית** — `squid-game-cover-20260626.png` (האינפוגרפיקה הריבועית «קוד המציאות»).
- **תיק מלא** — `squid-game-full-20260626.png` (האינפוגרפיקה הארוכה) משולבת בגוף.

## קבצים
- `post-content.html` — תוכן הפוסט (בטוח-לפרודקשן; URLs מרוחקים ל-storage). זהה למה שב-staging.
- `preview-final.jpg` — צילום רינדור מלא בסימולציית פרודקשן (אומת ויזואלית).
- `hero.svg` / `pyramid.svg` — גרפיקות SVG שבניתי (לא בשימוש בפוסט; צוריאל סיפק תמונות עשירות יותר).

## גימטריה — הכל אומת במנוע (רגיל / מסתתר)
| ביטוי | ערך | מקור |
|---|---|---|
| דיונון = עבדים | 126 | «דיונון» |
| בינה = ניאו | 67 | שחקן 067 · גשר למטריקס |
| ירח = זוהר | 218 | שחקן 218 · ירח=מלכות דוד/לבנה (פרשנות; לבנה עצמה=87) |
| פרצוף | 456 | שחקנים/פרס/זוכה |
| גאולה | 45 (רגיל) · 56 (מסתתר) | איחוד הגלוי+הנסתר ← 4·5·6=456=פרצוף |
| א | 1 | שחקן 001 (הבורא) |

החידוש המרכזי: **רגיל=החיצוניות (הפרצוף), מסתתר=הפנימיות (הנסתר)** — וגאולה מחזיקה את שניהם (45+56), וספרותיהם חופפות ל-456=פרצוף. הוסבר בפוסט מקטע «מה הן השיטות» + «הקוד הסמוי של גאולה».

## מנגנון התזמון (טכני)
- אין עמודת `status`/scheduling במערכת; הפרונט לא מסנן `date<=now()` → אי-אפשר פשוט להזריק עם תאריך עתידי (יופיע מיד בראש).
- לכן: התוכן נמשך מ-storage לטבלת `_sched_squid_456` (extension `http`), פונקציית `public.fn_publish_squid_456()` מזריקה ל-`posts` (id=5004, wp_id=36946) ואז `cron.unschedule`. job: `publish-squid-456` · `0 10 28 6 *`.
- אומת ב-dry-run (BEGIN…ROLLBACK): השורה נבנית תקין, `modified` לא null, `ai_touched=true`, `verified=true`.

## לביטול/שינוי לפני ראשון
- ביטול: `select cron.unschedule('publish-squid-456');`
- שינוי תוכן: לעדכן `post-content.html`, להעלות מחדש ל-storage, ואז `truncate _sched_squid_456; insert into _sched_squid_456 select content from extensions.http_get('<url>');`
- שינוי שעה: `select cron.alter_job((select jobid from cron.job where jobname='publish-squid-456'), schedule => '<cron>');`

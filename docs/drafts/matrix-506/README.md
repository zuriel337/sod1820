# הצופן הנסתר של מטריקס — 506

מקור התוכן של הפוסט `matrix-506` (posts.id=5003, wp_id=36945). הקנוני חי ב-DB (`posts`); הקבצים כאן = גיבוי/מפרת לשחזור (הסוכן הקודם איבד אותם כי לא היו ב-repo).

- `hero.html` — ההירו התלת-מימדי (קוד עברי נופל + 506 + 4 כרטיסי ציר). מחלקות בלבד, חסין לכלל המחיצה של תוכן WP.
- `body.html` — גוף הפוסט (אינטרו, ריבוע גימטריה מאומת, חיבור למטריקס, מספרי הסרט, ראו גם).
- `full-content.html` — hero+body מאוחדים (= posts.content).
- `preview.png` — צילום מסך אימות (לא נמחץ).

גימטריה (מאומת fn_ragil/fn_miluy): מילוי-בלבד=506 → סוד/כוח/דעת/מערכת · רגיל=506 → עולם המחשבה/סתום/העלאת/גאולתינו/מלכותי.

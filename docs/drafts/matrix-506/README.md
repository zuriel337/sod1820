# הצופן הנסתר של מטריקס — 506

מקור התוכן של הפוסט `matrix-506` (posts.id=5003, wp_id=36945). הקנוני חי ב-DB (`posts`); הקבצים כאן = גיבוי/מפרת לשחזור.

## נכסים
- `cover.jpg` — הכריכה הירוקה (האינפוגרפיקה של צוריאל, מוקטנת ל-900×600). מאוחסנת ב-Supabase storage bucket `gallery` תחת `posts/`, ומשמשת כ-`image_url`.
- `archetypes.svg` — גרפיקת «ארכיטקטורת 506 — ארבעת הארכיטיפים» (מורפיוס/סוד · האדריכל/מערכת · האורקל/עולם המחשבה · ניאו/כוח+גרזן). מאוחסנת ב-storage ומוטמעת בתוכן כ-`<img>`.
- `full-content.html` — תוכן הפוסט המלא (= posts.content). **בלי `<style>`** — רק inline styles + `<img>` (כי `<style>` בתוך content לא מרונדר בפרודקשן).
- `preview.png` — צילום אימות של הפוסט המלא.

## עקרון רינדור (חשוב)
בפרודקשן בלוק `<style>` בתוך `content` של פוסט **לא חל**; רק `style=` inline ו-`<img>` אמינים. גרפיקות = SVG/תמונה ב-storage, מוטמעות כ-`<img>` (לא inline-svg — ה-number-linkifier שובר ספרות בתוך SVG).

## גימטריה (מאומת fn_ragil/fn_miluy)
- רגיל=506: עולם המחשבה · סתום
- מילוי בלבד=506: סוד · כוח · מערכת · גרזן (גרזן: 766−260=506)

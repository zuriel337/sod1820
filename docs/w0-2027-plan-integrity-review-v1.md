# SOD1820 — W0 2027 PLAN INTEGRITY REVIEW / CONTINUATION CHECKPOINT

Date: 2026-09-09
Status: AUTHOR SELF-REVIEW · AUDIT/RECOMMENDATIONS ONLY · W0 NOT CLOSED · NO PRODUCT IMPLEMENTATION · NO MERGE/DEPLOY
Primary reviewer: GPT. Human Gate: ZURIEL.
BEFORE: work_log `8081cbd8-fa49-4ffd-b348-c087052456df`.
Existing independent specialist handoff: `7674e7b0-53d7-4a82-8b0d-43453630efc7` — QUEUED, not claimed executed.

## 0. Purpose and authority

ZURIEL requested a deeper review while Claude is unavailable and asked not to lose context. This report preserves the decision-changing findings and an exact continuation point. It is not another Roadmap, System Frame, registry, state store or Foundation contract.

OWNER CHECK = EXTEND_EXISTING. Relevant owners actually read: System Frame; Research Studio; Master State governance/pointer section; Owner Index; active Experience Governance v1, Foundation Closure v2, Coordination v6, Work-log Authority v2, Live State Resolution v2, Truth Axes v3, Raziel Companion v1 and deploy_on_request v1.

The approved product direction remains: replace presentation freely while preserving capabilities, identities, truth, provenance, access and Human-Gate decisions. A newer clear product decision should be reconciled into its existing owners; old presentation clauses must not silently veto it, and a branch Roadmap must not silently override owner contracts.

This is GPT reviewing a plan GPT helped write. It does NOT substitute for the required independent Claude challenge. No new specialist task was dispatched. No product data, canonical rule, owner contract, Roadmap or Product Map was modified by this review.

## 1. Verified snapshot and scope

| Evidence | Verified snapshot / result |
|---|---|
| origin/main | `cf4d8df54cde53a22f5d217d2f79c11f0f734e13` |
| Roadmap/Product Map/W0 branch before this report | `gpt/roadmap-v5-4-product-map-2027` at `6bec33b879666a0ed7d55d1543e28d953cdd25b6`; 6 ahead / 0 behind main |
| Original World blueprint/challenge branch | `gpt/world-command-room-v1-blueprint` at `50525313e827aaafbdebde5eea8e36fa0fd47264`; 4 ahead / 0 behind main |
| Main Roadmap | v5.3, blob `013b9ef35a6530acc85d42de9d743c32cc931b8e` |
| Archived v5.3 on Roadmap branch | same blob `013b9ef35a6530acc85d42de9d743c32cc931b8e` — byte-preservation verified |
| Proposed v5.4 Roadmap | blob `8047bbfead6cb547868d93b548850ba9c44f20c1` |
| Product Map | blob `e49f6edd5b5f3e1ef397753bd9058de5123b8c97` |
| Orientation requirements | blob `36ca53efdeb1fd47effb77b35b9094f46a338a72` |
| Main System Frame | blob `429ced4cc4301c1f232342696947431d9f93604b` |
| Main Research Studio | blob `05b4b861e6050beab28fc7e32e6e4843eb4634f9` |
| Main Research Context normalizer | blob `ce833fec3d3bee71c6872f8b0fa215576cf97311`; 4,088 bytes |
| Canonical Supabase | `linswmnnkjxvweumprav` only; live rule and coordination reads; no research-content rescan |

Read/reconciled: the proposed v5.4 Roadmap, full Product Map, route-family W0 inventory, orientation note, original maximum-challenge report, relevant owner bodies and current Research Context code. The initial macro crosswalk and original blueprint are preserved dependencies, not completed exhaustive capability inventories.

Scope limitation: this is plan/owner/code-boundary review, not a full route-by-route product audit, authenticated browser acceptance, production security audit or performance/load benchmark. No 100M-entity scalability claim is established. Existing ELS/Explorer continuity was not re-audited or declared broken.

## 2. Verdict

The one-world/many-views direction remains worth pursuing. The main improvements now are not additional features: they are reconciling authority, removing a circular gate, preserving old open obligations, and making the experience predictable and permission-safe.

**W0 remains NOT CLOSED.** No evidence in this bounded review justifies a new global graph, store, Context owner, workflow registry or AI memory system. This is not a blanket re-certification of every underlying Foundation or runtime.

## 3. Confirmed gaps and bounded recommendations

### PIR-01 — Owner reconciliation is broader than the Bottom Dock

Evidence: System Frame still prescribes a fixed Dock/three-layer frame and an immersive Heichal. Research Studio §7 references the three-column Workspace; §8 explicitly says Number Page is never rebuilt/replaced and its locked findings area is not redesigned. Proposed v5.4 explicitly allows replacing these presentations.

Finding: confirmed DOCUMENTATION/OWNER DRIFT. Reconciling only System Frame would leave a second conflicting instruction in Research Studio and its cited preservation clauses.

Recommendation: one finite clause-level reconciliation under existing owners. Supersede presentation-only locks according to ZURIEL's newer direction; retain capability, identity, provenance, engine behavior, access, working deep links and user data obligations. Trace cited Research DNA clauses only where needed to reconcile the same conflict. Do not reopen the underlying research Foundation or create a competing shell law. Master State gets a pointer/status update, not copied contract bodies.

Gate: before W0 owner acceptance/W1 implementation. Recommendations not applied here.

### PIR-02 — W0 has a circular acceptance dependency

Evidence: v5.4 §6 prohibits the large Shell build before W0 PASS. Orientation requirements §8 say W0 is not closed until World→Heichal→Tool→World continuity is proven in the 1237 Golden Slice, which is a later implementation.

Finding: literal gate dependency is circular. The product evidence is valuable; its timing is wrong.

Recommendation: W0 closes the specified behavior, owner mapping, exact state fields/references and test plan, with small diagnostic probes where useful. W1/W2 provide executable shell/read-only continuity evidence. Later write/AI/paid slices provide their own acceptance evidence before release. Design acceptance must not claim implemented acceptance, and implemented acceptance must not claim deployment.

Gate: before W0 closure. Do not resolve the loop by marking unbuilt checks passed.

### PIR-03 — Archiving v5.3 preserves bytes, not operational routing of every open task

Evidence: old Roadmap and archive have identical blob SHAs. The new v5.4 replaces the active navigation with W0–W9 and a short foundation posture. The inspected route inventory explicitly remains family-level, not an exact ledger. The former P1–P6 program included non-number portability, Method Inspector, entity-aware Search/SEO and multilingual projections.

Finding: no history loss was found. An exhaustive old-open-item→new-home/status/next-action crosswalk is not yet demonstrated. Missing crosswalk is not proof those tasks were completed or intentionally retired.

Recommendation: preserve one brief carry-forward table under existing Roadmap/W0 navigation: old workstream or gate → existing owner → actual status/evidence → W-stage or retained separate track → next action. Include current independent obligations such as the bot-surface experiment, authoring/identity and account work only where still open; verify each before assigning state. W0–W9 is the experience program, not automatic replacement of every site workstream.

Gate: before calling v5.4 a reconciled whole-project navigation release. This is finite reconciliation, not a new full source scan.

### PIR-04 — Orientation needs one consistent, truthful contract

Evidence: Product Map/Header says focus/breadcrumb 'when useful'; the newer orientation note calls it persistent. Reading-mode text allows suppressing most chrome. The user has prioritized an always-clear top location anchor.

Recommendation: a compact top orientation anchor persists; optional controls and older path segments may collapse. Any genuine fullscreen exception must be explicit and recoverable, never a silent loss of place. Preserve stable control locations within a device/mode rather than moving controls during an active task.

Distinguish four things: where the content belongs, what is focused now, how this research session arrived, and the saved Journey path. They are not necessarily the same chain. Visiting Number→Book is not evidence of a canonical Number↔Book relationship. Direct entry has no invented previous research root. Display current location and 'research started at' separately when needed.

Gate: W0 semantics; W1 acceptance. No new orientation store.

### PIR-05 — Research Context is reusable, but not an arbitrary future snapshot container

Evidence: the exact main normalizer was copied to a local diagnostic environment and its Git blob SHA verified before execution. Node v22.16.0 executed 9 assertions.

Observed retained fields: subject/root, current selection, exact locator, scalar Explorer facet/offset, return URL including query, Journey position.

Observed unsupported diagnostic extensions discarded by v1: nested `dimensions.toolState`, `journey.revisionId`, and `returnTo.selection`.

Interpretation: v1 behaves according to its current shape. Merely adding arbitrary future World state to it is not a valid exact-reopen implementation. This is NOT a finding that existing ELS/Explorer reopen is broken; they may encode their governed state through existing URL/snapshot/reference paths.

Recommendation: explicitly map W1/W2 state to existing owners and compatible fields/references, extending the existing adapter/version only where necessary. Define same-session return, reload restoration and saved Journey reopening separately. A stale/deleted/restricted result must yield an honest unavailable/changed state, never silently substitute another result.

Gate: minimal concrete state crosswalk before shell implementation; round-trip tests before acceptance. No new persistence system inferred.

### PIR-06 — World→Heichal→Tools is not a mandatory three-click navigation sequence

Evidence: the hierarchy correctly separates knowledge from research actions; the roadmap still illustrates flows through World before a tool.

Recommendation: direct links to a calculator, ELS, Book or source remain first-class. A visitor should be able to use a tool without opening a graph, learning architecture vocabulary or talking to Raziel. The same shell/context is established by the entry adapter. An in-page quick action and a full Heichal workspace may invoke the same engine.

Capability compatibility must be explicit: a number used as research root is not automatically an ELS search term; a scanned Book is not automatically an approved exact corpus for position-sensitive computation. Registered methods are not all executable. Missing compatible input yields a clear selection/unsupported state, not a fabricated run or graph edge.

Gate: action prerequisites in W0; specific engine/corpus checks at the applicable tool gate.

### PIR-07 — Persistence, sharing, paid tier and authority are different questions

Evidence: Product Map groups 'shareable/reopenable'; Access table lists Public/Premium/Deep/Admin as rows. Truth Axes v3 independently requires caller authority and publication/access boundaries. Research Studio preserves personal state through existing owners.

Recommendation: reopening privately never implies permission to share. Separate state lifetime from audience. Separate signed-in identity, ownership, paid entitlement, publication/privacy, operational role and canonical-promotion authority. Admin is not a fourth purchasable tier. A signed-in free user can still own private account/research state where current owners allow it; paid depth grants no other person's inbox.

URLs, breadcrumbs, telemetry, Raziel context, cache and external handoffs must not disclose private source IDs/text merely to make navigation convenient. Context `access` labels are display context, not authorization evidence. Revalidate on logout, account switch, entitlement change and privileged action.

Gate: W0 access/state matrix; protected-data and paid exposure tests before the respective launch. Detailed commercial packaging remains unratified.

### PIR-08 — One Tree is not one table, one pipeline or one universal Inbox

Evidence: Product Map §10 presents all creation flowing through Intake to World. Research Studio §0/§3 explicitly preserve Gate #18 Option C: shared judgment contracts over existing pipelines, no physical pipeline consolidation. Product Map already distinguishes personal attention/global Now/research inbox; this distinction must govern the diagram too.

Recommendation: ordinary DM/comment/account action stays in its domain. Only explicitly submitted/eligible research material enters the governed research intake path. Editors may share commands and review principles without merging their persistence/lifecycles. Notification read state, moderation state, candidate approval, canonical promotion and publication remain separate.

Likewise, My Workspace is a composition of personal state and references, not a forced copy into the Reality Graph. Operations/Metatron diagnostics and billing are shell-integrated domains, not knowledge nodes. Preserve the existing Name-as-string vs identified Person distinction; a profile is not automatically a public Person entity.

Gate: W0 personal/creation/action crosswalk. No domain pipeline rewrite authorized.

### PIR-09 — AI-native must stay user-controllable and work without AI

Evidence: the prior challenge already warns against unpredictable personalization and preserves stable command/search entry. System Frame protects quiet Raziel behavior and forbids generated activity from opening/taking over the screen.

Recommendation: visible stable navigation, Search, return, manual tools and My Workspace remain usable during an AI timeout, quota exhaustion or disabled companion. Raziel can recommend layouts and invoke approved actions; he must not invent arbitrary executable UI, silently remove controls, change semantic identity or perform privileged mutations. An adaptive shell changes presentation at clear task/device transitions, not while the user is aiming at a control.

Keep 'continue on your own', cancel and reset-to-default available. The default experience is calm; advanced composition is on demand. Maintain keyboard/list equivalents and focus/reading position.

Gate: W0 interaction rules; negative AI-unavailable tests in W1/W2. No new feature family needed.

### PIR-10 — The first Golden Slice is currently closer to a whole program

Evidence: v5.4 §7 includes sources/Books, tools/ELS, Journey, Raziel and Admin Candidate→Human Gate while W3–W7 build much of that. W7 is also where full Raziel/context implementation is scheduled. The initial challenge conditions its broad future-proofing verdict on this large flow.

Recommendation: retain the complete journey as cumulative program acceptance, not the entry ticket to showing any useful screen. First bounded preview: one real number/entity, persistent orientation, one real available source or finding, a compatible existing tool/reader, the same personal-research entry and exact return; prove desktop/mobile/keyboard and honest empty/error states. Do not invent an ELS or Book link to complete a showcase.

Specify the minimal Raziel context-consumer boundary early; full conversation/tool orchestration remains later and gated. An inert companion slot is not evidence of AI integration. Candidate writes, canonical actions and paid payloads have separate gates. Each later slice must produce visible value and preserve previous acceptance.

Gate: W0/W1 sequencing correction. This report does not authorize implementing or releasing the preview.

### PIR-11 — The planning package is preserved but split across branches

Evidence: original blueprint/challenge are on `gpt/world-command-room-v1-blueprint`; v5.4/Product Map/W0/orientation are on `gpt/roadmap-v5-4-product-map-2027`. Both are ahead of the same main; neither is main. The latest coordination task requires an independent read-only challenge. The original challenge described another challenge as optional, while active coordination v6 requires it for this contract/redesign risk.

Recommendation: use the current Roadmap branch as the planning entry and point to original artifacts as lineage; perform a unique-requirement crosswalk before a consolidated docs release. Avoid leaving two active specifications that say different things. Link the orientation and this review through W0/coordination; do not rely on chat recall or on a file merely existing in a branch.

Important: a work_log handoff being queued does not start Claude. On an available session, Claude reads the existing handoff plus this supplement. GPT then reconciles the independent result. No second dispatch, messenger duty or background execution is implied.

Gate: before canonical documentation release/W0 PASS.

### PIR-12 — Performance and release quality need measured acceptance, not year labels

Evidence: the challenge uses 100M as a thought experiment; the plan prescribes bounded queries without a measured workload budget. Premium anti-scraping/rate budgets appear under Later while W8 is paid-depth launch.

Recommendation: '2027' is a target, not evidence of product quality. Define a bounded acceptance workload and measure load time, interaction responsiveness, memory, result completeness and query cost for the actual first surface. No present 100M capability claim. Client truncation must be visibly distinguishable from an empty/exhaustive result.

Before protected/paid-data release, rate/cost limits, least-privilege readers, cache isolation, access revocation and abuse controls must already be decided/verified for that slice; they are not optional post-launch decoration. Later richer anti-abuse systems need not block a public read-only preview.

Interrupted candidate saves must be idempotent and show truthful saved/unsaved state; concurrent edits must not silently overwrite a Human-Gate decision. This is scoped acceptance under existing owners, not a proposal to build a universal job/transaction system.

Gate: specify baseline acceptance during W0; measure/enforce before the affected slice's release.

## 4. Finite adversarial acceptance scenarios

These are required scenarios to specify now and execute at the relevant slice, NOT tests claimed already passed:

1. Enter a Book/ELS deep link directly without a previous research root; header remains truthful.
2. Number→source→tool→return restores meaningful state; visiting content never manufactures a graph relationship.
3. Open My Workspace and return; personal selection does not replace research root unexpectedly.
4. Raziel fails/is disabled; manual navigation/tools/return still work.
5. Open private research as another user or after logout; no private payload/URL/cache/AI-context leakage.
6. Candidate save times out and is retried; no duplicate mutation and no false 'saved/canonical' badge.
7. Source/result changes or is unavailable; exact reopen explains the mismatch, never silently re-runs as if identical.
8. Mobile/keyboard/screen-reader use preserves orientation and action parity without drag or graph-only interactions.
9. Paid entitlement/role changes; data is reauthorized, not merely visually hidden.
10. Second tab/research path changes; current task ownership and unsaved work do not silently cross sessions.

## 5. Next work, with a stop condition

Next: a bounded W0 reconciliation package, not another expansion of the vision.

- Reconcile presentation-only clauses under System Frame AND Research Studio/cited owners; preserve the invariant obligations.
- Carry old open workstreams/gates forward into v5.4 navigation with evidence and one destination per item.
- Complete the exact route/alias and personal/action/state/access crosswalk; keep communication and account domains outside automatic research intake.
- Remove the W0↔Golden-Slice circular gate and separate design, implementation and release acceptance.
- Obtain the existing independent Claude challenge, then GPT live reconciliation and ZURIEL acceptance where decisions are needed.

Stop the planning pass once these finite gates are resolved. Do not repeatedly audit completed source corpora or keep adding futuristic capabilities. Then build the bounded first product proof, still branch-only until explicit release authorization.

## 6. Resume without chat memory

Read this report, then the current versions of:
- `SOD1820_MASTER_OWNER_INDEX.md` on main and only relevant active rule bodies;
- `docs/sod1820-system-frame-contract-v1.md` and `docs/research-studio-v1-contract.md` on main;
- proposed v5.4 Roadmap, Product Map and W0/orientation documents on the Roadmap branch;
- existing handoff `7674e7b0-53d7-4a82-8b0d-43453630efc7` and this review's BEFORE/AFTER.

Refresh branch/main and parallel-writer state before any write. Preserve user decisions from the planning artifacts; do not assume branch artifacts are canonical/main. The open System Frame BEFORE is not proof a reconciliation was applied.

Release status of this review: report written on a branch only. No owner reconciliation performed, no W0 PASS, no code/schema/data promotion, no merge, no deployment, no production browser verification.

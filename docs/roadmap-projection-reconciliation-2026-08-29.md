# SOD1820 — Roadmap Projection Reconciliation · 2026-08-29

Status: NAVIGATION RECONCILIATION CANDIDATE · docs-only · Human Gate required for merge.

## Superseding navigation

The v5.3 Roadmap still points to `INTAKE READINESS CLOSURE`. Live verification on 2026-08-29 shows that return point is complete: Research Intake Foundation is closed, the Biblical Cipher Research Foundation contract is merged, and the One Tree Research Object → Graph identity gate has been repaired and passed an idempotent replay proof.

**Foundation verdict:** Research Intake — SUFFICIENT. Biblical Cipher research contract — SUFFICIENT to defer heavy implementation. One Tree projection identity — SUFFICIENT.

**Proposed current navigation:** `ACTIVE_NOW: WS-RESEARCH-STUDIO-PROJECTION`.

## Immediate build target

Follow the approved Research Studio v1 build order. The next step is to extend the existing Global Research Workspace (`ResearchProvider` / `ResearchCenter`) with the approved Universal Finding envelope and a reusable Research Surface projection. Do not create a second Workspace, Graph, Context store or source engine.

The first acceptance slice should be small and read-mostly, with heterogeneous source-native findings such as Number/Gematria, Graph/Relation, ELS and a durable Research Object where appropriate.

Acceptance requirements:
- one shared Finding envelope;
- stable source-native identity;
- provenance visible;
- truth/verification state remains explicit;
- no automatic Finding → `research_object` creation;
- no automatic canonical/published promotion;
- pin/save means Workspace membership, not truth promotion;
- same state remains reusable by later Number, Entity, ELS, Person, Time, Layered and 3D projections.

## Classification

**MUST NOW:** reconcile navigation; build the smallest Universal Finding → existing Workspace projection slice; preserve identity/provenance/truth lifecycle end-to-end.

**EXTENSION POINT NOW:** reusable Research Surface contract; renderer-independent dimensions/depth metadata; ranking/explanation hooks.

**LATER:** Number Page visual redesign; full ELS massive-discovery implementation; Person/Year Journey experience; cinematic 3D/Research Universe; broad navigation/mobile/multilingual redesign.

## Do not do now

No new Foundation audit without contradictory evidence. No bulk Research Object promotion. No new graph/store/context table. No parallel ELS/3D engine. No legacy-UI-driven schema changes. No deep research detour in place of Projection work.

Foundation → Projection → Experience.

Preserve capability, truth and provenance — not necessarily the legacy interface.

This addendum preserves the old Roadmap text as history. After Human-Gate approval, its superseding navigation should be absorbed into the Master Roadmap and Master State rather than deleting historical provenance.

# פרוטוקול בדיקות — הצלבת שיטות + שכבת הישויות

מטרה: לוודא שהמנוע (חישוב · הצלבה · ישויות · גרף · נשמת-שיטה) נכון ויציב **לפני** שמקבעים הגדרות.
סטטוס: 🟢 A · D · F · G1 + B(נתונים) + E עברו. נותרו לתצוגה ב-Preview: B2, C, G2. מסמנים `[x]` כשעובר.

איך מריצים: בדיקות נתונים (A,B,D,E) — דרך SQL/MCP כאן בצ'אט. בדיקות תצוגה (C,F) — דרך `/cross` ב-Preview.

---

## A · דיוק החישוב (8 השיטות)
ביטויי-זהב עם ערכים ידועים. כל שיטה חייבת להתאים בול.

- [x] **A1 · עוגן "התגלות"** = רגיל 844 · מילוי 1026 · מסתתר 1237 · קדמי 3137 · גדול 844 · סידורי 70 · אתב״ש 392 · אלב״ם 241.
- [x] **A2 · סופיות** — `מלך`: רגיל 90 אבל גדול 570 (ך=500). `אברהם`: גדול 808 (ם=600) מול רגיל 248.
- [x] **A3 · מסתתר** — `חכמה` מסתתר = 67 (=בינה ברגיל). `דוד` מסתתר = 4.
- [x] **A4 · ניקוי קלט** — רווחים/גרשיים/ספרות מסוננים: `רבי עקיבא`, `האר״י הקדוש` מחושבים על האותיות בלבד.
- [x] **A5 · עקביות מול האתר** — אותם ערכים כמו `GematriaCalculator` ו-`src/lib/gematria.js`.

## B · הצלבת השיטות
- [x] **B1 · 1820** מחזיר את הביטויים הנכונים בכל שיטה (רגיל: בשתי שבתות, תורתי אמת המשיח…).
- [ ] **B2 · מספר ללא התאמות** → מצב ריק נקי ("אין ביטוי מאומת…"), בלי קריסה.
- [x] **B3 · ללא כפילות** — ביטוי שנופל על המספר בכמה שיטות נספר פעם אחת ב"ביטויים מאומתים".
- [x] **B4 · התכנסות מלאה** — מספר שנופל בכל 9 השיטות (313/326/322) מציג "✦ התכנסות מלאה".

## C · שכבת הישויות (תצוגה ב-/cross)
- [ ] **C1 · דירוג** — "המסר המרכזי" ממוין לפי משמעות (★5 בראש).
- [ ] **C2 · תיאורים** — המשפט המתאר מוצג תחת כל ישות שיש לה.
- [ ] **C3 · ישויות קרובות** — נשלפות מהגרף (edges), בלי כפילות עם המסר המרכזי.
- [ ] **C4 · ניווט** — לחיצה על ישות מובילה לדף הישות (`/number/<label>`).
- [ ] **C5 · מספר ללא ישויות** — הסעיף נעלם בחן (לא קופסה ריקה).

## D · איכות הנתונים (ישויות)
- [x] **D1 · 8 שיטות מלאות** — כל `gematria_word` של ישות עם כל 8 העמודות (אפס null).
- [x] **D2 · קישור** — לכל ישות (`nodes type=entity`) יש `gematria_word` עם `node_id` תקין.
- [x] **D3 · אין כפילויות** — אין שתי ישויות עם אותו `label` + source.
- [x] **D4 · שלמות הגרף** — כל `edge` מצביע לשתי ישויות קיימות.
- [x] **D5 · הפיכות** — כל הזרע מתויג `source='entity_seed_v1'` (ניתן למחיקה אחת).

## E · מקרי קצה
- [x] **E1 · ערכים חריגים** — 0 / שלילי / מספר ענק → לא קורס, מצב ריק.
- [x] **E2 · גרשיים בשם** — `האר״י הקדוש`, `רבי עקיבא` מתחברים נכון (לא נשברים על `״`/`'`).
- [x] **E3 · ביטוי לא קיים** — שם שאין לו ערך → מסר ריק מנומס.

## F · נשמת השיטה (אתב״ש ↔ אלב״ם)
- [x] **F1 · אתב״ש = מראה/היפוך** — חיבור דרך אתב״ש מסומן/מפורש כ"הצד הנגדי".
- [x] **F2 · אלב״ם = בן/בת זוג** — חיבור דרך אלב״ם מסומן/מפורש כ"הזיווג המשלים".
- [x] **F3 · נשמת כל שיטה** — לכל אחת מ-8 השיטות יש "נשמה" מוגדרת (טבלת המשמעויות).

## G · רגרסיה
- [x] **G1** — `npm run build` עובר.
- [ ] **G2** — `/cross` נטען, RTL תקין, בלי שגיאות קונסול.

---

### ממצאים פתוחים
- 2026-06-14: A,D,F,G1,B1,B3,B4,E1,E2,E3 עברו (נבדקו ב-SQL/קוד). נותרו B2/C1-C5/G2 — דורשים Preview.
- תוקן ב-D1: ארץ ישראל/בית המקדש (מסתתר קנוני).

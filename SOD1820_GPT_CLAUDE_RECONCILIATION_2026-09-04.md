# SOD1820 · GPT ↔ Claude Canonical Reconciliation — 2026-09-04

**Status:** Candidate reconciliation document — READ-ONLY output. No code, schema, RLS, branch, CLAUDE.md, PR, or production change was made in producing this document. Supersedes nothing until ZURIEL reviews it; sits alongside `docs/canonical-reconciliation-2026-08-29.md` and `docs/roadmap-projection-reconciliation-2026-08-29.md` as the same class of artifact.

**Trigger:** ZURIEL flagged that the prior `SOD1820 FULL SYSTEM AUDIT` (same day, earlier session) treated a large amount of real, ongoing GPT work as an "undocumented parallel operating system" without reconstructing GPT's actual work history first. This document does that reconstruction and re-judges the affected audit sections.

**Method:** 4 independent read-only research passes — (1) full `work_log`/`decision_ledger`/`researcher_definitions`/rules chronology since 2026-08-01, not just the last 15 rows; (2) full field-level breakdown of `research_objects` growth (124→680); (3) branch-provenance re-check (MASTER_CLASSIFICATION v1/v2/v3, plus 12 other flagged branches, checking whether "unmerged" really means "missing from production"); (4) a data-flow/contract trace of whether Research Objects/Human Gate/Truth Axes/PR-workflow is the same Research OS or a different one. No SQL write, no git write, no merge, no deploy was executed by any pass.

---

## 1. Executive Summary

The prior audit's central claim — "a parallel, undocumented GPT+ZURIEL operating system" — **does not survive full reconstruction of the evidence**. Every concrete piece of that claim traces back to the *same*, already-documented Research OS architecture (`research-studio-v1-contract.md` / `research-universal-finding-contract.md`, PR #187, merged 2026-08-24, already folded into `SOD1820_MASTER_STATE.md` §0-A one day *before* most of the "mysterious" GPT activity even happened). `research_objects` and `decision_ledger` are pre-existing tables (oldest rows 2026-07-23/07-30, before GPT appears in `work_log` at all). "Human Gate" is a pre-existing SOD1820 governance term (git evidence from 2026-08-11, referenced in `CLAUDE.md`'s `command_center_law`), not GPT vocabulary. "Truth Axes" is one rule (`truth_axes_foundation_law` v1), written once on 2026-08-29, explicitly self-described as extending an older rule. Every `gpt/*` PR merges into the same `origin/main`, same sequential PR numbering, same repo Claude uses (PR #166→#309, interleaved with `claude/*` merges).

What actually happened: GPT ran a real, disciplined, mostly-gated 21-day build/research sprint (2026-08-12 → 09-04, ~350 work-log items) that Claude's audit sampled only the last 15 rows of and mistook for an unexplained anomaly. Several of the audit's *other* findings hold up fine after this correction (see §5); a few need outright reversal; a few need nuance. None of the newly-confirmed GPT work invalidates the parts of the original audit that were about frontend code, the `research` Postgres schema, RLS gaps, or the identity-resolution subsystem — **those were not re-investigated in this reconciliation round** and their original verdicts stand unchanged (flagged explicitly below, not silently carried over).

**What changed:** the "parallel system" framing, the "gematria-lists-organization-u39nlj is the largest at-risk artifact" framing, most of the "stranded branch" framing, and the "research_objects growth is unexplained/very active" framing.

**What did not change:** the missing canonical UI components, the open Command Center CC-1 target ambiguity (though now understood as active WIP, not neglect), the `fn_mem_add`/`fn_raziel_fact` security gap, the `convergences`/`project_codex` RLS-no-policy findings, the 63%-orphaned-number-nodes graph finding, the undocumented `research` schema and identity-resolution subsystem, the Gemini-default-engine disclosure gap, and the `OPENWEB_SSO_SHARED_SECRET` mystery.

---

## 2. GPT Change Ledger (2026-08-12 → 2026-09-04)

Full per-day narrative (all ~350 raw `work_log` items) was reconstructed and is available in full if needed; this table condenses it to every item that touched a PR, a rule, a schema, or a Human Gate decision. Dates with only design-memo/coordination traffic (no PR/schema/rule) are omitted from the table but are accounted for in the totals below it.

| Date | GPT work item | Purpose | DB/code/docs affected | PR/commit | Human Gate | Canonical status | Production status | Supersedes/replaces | Evidence |
|---|---|---|---|---|---|---|---|---|---|
| 08-12 | WhatsApp intake + research command-center coordination memo | Align Claude/GPT on scope | none | — | n/a | memo | n/a | — | `work_log` |
| 08-14 | `actor=GPT`/`actor=CLAUDE` tagging convention begins | Attribution hygiene | `work_log` | — | n/a | — | — | — | `work_log` |
| 08-15 | ELS Cycle-1/1B search-space integrity | ELS correctness | `tools/els/` (branch) | `claude/els-6jjt71` | pending | branch-only | **not merged, not deployed, main untouched** (self-declared) | — | `work_log` |
| 08-17 | "Research Object Map" design memo (first-ever use of the phrase) | Architecture design | docs only | — | n/a | design memo | none | — | `work_log` 03:53 |
| 08-19 | "3D Knowledge Universe" visual preview | UX exploration | branch preview | — | n/a | preview | **not merged, no deploy** (self-declared) | — | `work_log` |
| 08-20 | 6× read-only audits (ELS inventory, site×master-map cross-map, Discovery Control Center flow) | Understand current state before building | none (read-only) | — | n/a | audit | n/a | — | `work_log`, attributed `actor=CLAUDE, requested by GPT` |
| 08-23 | ELS Lifecycle Contract → Matrix Snapshot → Native Renderer → Hidden Engine Bridge → Verse/Context Lens → Research Journey Core | ELS feature build, PR-gated | `tools/els/`, `src/` | **PR #174, #178, #179, #180, #181, #183, #184, #185** | gated per-PR | #185 explicitly "merged/deployed/verified" | live | earlier ELS-branch prototypes | `work_log` + `git log --merges` |
| 08-24 | **Research Studio v1 Human-Gate approved** → contract docs merged | Canonical architecture: "one Research OS" | `docs/research-studio-v1-contract.md`, `docs/research-universal-finding-contract.md`, `SOD1820_MASTER_STATE.md` §0-A | **PR #187** | **approved by ZURIEL** | canonical architecture decision | live, folded into Master State §0-A same reconciliation cycle | supersedes ad-hoc "Research DNA v1" framing (drift explicitly flagged and resolved same day) | `work_log`, Master State §0-A |
| 08-26 | Numeric Research Router v1 (π, then Fibonacci) | New research-router capability | `nodes`, `research_objects` | **PR #200 merged**, PR #203 opened | gated | live-verified | live | — | `work_log`, git |
| 08-26 | **`corpus_admission_lifecycle_law`**, **`corpus_admission_foundation_v1`** rules + `project_codex` entries | Close gematria-word admission gaps (VIP bypass, RLS perimeter, unauth `add_entity`) | `gematria_words`, `nodes`, RLS policy | PR #196→#199→#200 | gated | "0 remaining gaps" (closure pass) | live | earlier ad-hoc admission logic | `work_log`, `docs/corpus-admission-foundation-v1-contract.md` |
| 08-27 | Research Intake "final freeze" | Freeze intake foundation before further build | `research_objects` intake path | merged (per log) | gated | live | live | — | `work_log` |
| 08-27 | SpatialResearchScene v1 | New 3D spatial research surface | `src/` | merged/deployed | gated | live | live | — | `work_log` |
| 08-29 | **`truth_axes_foundation_law` v1** ("M1 Truth/Epistemic Contract") | Formalize verification≠governance≠publication axes | `nodes`, `project_codex` (`truth_axes_foundation_v1`) | rule-only, no code | n/a (rule adoption) | canonical rule | live | none — explicitly extends, does not replace, prior intake rules | `work_log`, `nodes` |
| 08-29 | `foundation_closure_protocol_law`, `experience_governance_foundation_v1_law`, `engine_governance_registry_authority_law`, `research_object_identity_invariant_law` v1 | Same-day rule-freeze batch, each self-described as extending an existing rule | `nodes`, `project_codex` | rule-only | n/a | canonical rules | live | prior looser conventions | `nodes` timestamps 02:49–22:53 |
| 08-29 | Experience Governance Foundation — 3-pass Claude↔GPT cross-verification | Cross-check before merge | docs/schema | **PR #234 → #236** | Claude reported "changes requested, not ready" on pass 1, then approved | closed after 3 passes | live | — | `work_log` |
| 08-31 | Subscriber Attribution Wiring v1 | Attribution tracking | `nodes`/tracking | **PR #262**, live-checked against exact commit SHAs before merge | **Human-Gate-approved release** | live-verified | live | — | `work_log` |
| 08-31 | **Corpus-Intake Truth-Axis Separation** | Separate approval/verification/publication for word review | `resolve_word_review`, `fn_verify_gematria_word_engine` | **PR #266** | released | **"live-verified"** by GPT's own log | **live** (confirmed independently in the original audit's Books/Raziel pass — function exists and is called exactly as designed) | old conflated `resolve_word_review(approve)` | `work_log`, prior audit's independent DB check |
| 09-01 | Video Structured-Data Coverage | Content structuring | `video_transcripts`-adjacent | merged/deployed | **Human-Gate-authorized** | live-verified | live | — | `work_log` |
| 09-01 | Portable Research Context closure attempt | Cross-session research continuity | — | **stopped mid-write** — "no safe byte-preserving write" found | self-blocked, not forced | not implemented | not implemented | — | `work_log` (a real example of the workflow refusing an unsafe write) |
| 09-02 | **35-item PELIAH linguistic-cipher research sweep** | Systematic method-by-method decode research on ספר הפליאה | `research_objects` only | none (research data, not code) | n/a (candidate data) | **every single item closes `IMPLEMENTED_LIVE_VERIFIED_CHECKPOINT_SAVED_NOT_CANONICAL_NOT_PUBLISHED`** | candidate only | — | `work_log`, cross-confirmed by `research_objects` classification (42 Peliah rows) |
| 09-02 | Atbach Method Definition Reconciliation → Correctness Patch | Fix a real method-definition bug found during the sweep | `nodes` (method definition) | **released, merged to main DB, live-verified** | Human-Gate decision readied then executed | live | live | prior (incorrect) Atbach definition | `work_log`, with an interleaved Claude cross-verification/corrective-fix row same day |
| 09-03 | Gematria Method Readiness | Method-registry cleanup | `gematria_methods` | **merged to main, live-verified** | gated | live | live | — | `work_log` |
| 09-03 | Numeric Router Gematria-Trace | Router feature | `nodes` | **merged/deployed, live-verified** | **Human-Gate-approved** | live | live | — | `work_log` |
| 09-03 | "Professional Numeric Tools Foundation" — Claude cross-verification found a decision-changing drift | Quality control | foundation gate revised | n/a | Claude flagged, GPT revised | revised | n/a | prior (flawed) foundation gate | `work_log` — a real example of the Claude↔GPT cross-check loop catching a real bug |
| 09-04 | **System-Map Visual-Progress Fix v1** | Public build-map accuracy | `src/`, roadmap | **merged/deployed/live-verified** | gated | live | live | — | `work_log`, matches original audit's own observation of this exact banner |
| 09-04 | **Codes Reference Surface v1** | Dossier terminology fix | `src/pages/CipherPage.jsx`-adjacent | **PR #305, merged/deployed/live-verified** | gated | live | live | — | `work_log`, git merge log |
| 09-04 | `inter_agent_coordination_law` bumped v3→v4 | Formalize specialist-escalation between Claude/GPT | `nodes` (rule) | rule-only | n/a | canonical rule | live | v3 | `work_log` |
| 09-04 | Command-Room prototypes (Simple Workdesk v1, Zvi Golden-Case, One-Tree projection, source-map reference) ×5 | Iterating toward a Command Center UI | branch previews | none merged | n/a | preview only | **not merged, not production** (self-declared each time) | — | `work_log` |
| 09-04 | Human Gate Desk v1 | Process-drift found and repaired mid-build, then shipped as preview | branch | none merged | process gap self-reported and fixed | preview | **not merged, not production** | — | `work_log` |

**Totals for the window:** 1,398 raw `work_log` rows since 2026-08-01, of which 443 raw / ~348 collapsed items are GPT-tagged (tagging convention itself only starts 08-14) and 477 are Claude-tagged. GPT presence is **zero** from 08-01–08-11. 64 `gpt/*` branches exist; sampled ancestry checks split cleanly into "merged via a named PR" and "not merged, and the matching work_log row already says so" — no discrepancy found between what git shows and what GPT's own log claims about merge state.

---

## 3. What GPT Actually Built

- **Research Studio v1 / Universal Finding contracts** (PR #187, 2026-08-24, Human-Gate approved) — the architectural core: one Reality Graph, one Research Workspace, one Human Gate; `research_objects` formalized as one of exactly three storage homes.
- **Corpus Admission Foundation v1** (2026-08-26) — closed 5 real bypasses in gematria-word intake (VIP bypass, unauth `add_entity`, RLS perimeter gap, fuzzy-match false positives), "0 remaining gaps."
- **Truth Axes Foundation v1** (2026-08-29) — the formal four-axis law (epistemic type / verification / governance / publication) that later intake work conforms to.
- **Corpus-Intake Truth-Axis Separation** (PR #266, 2026-08-31) — separated `resolve_word_review`'s conflated approve/verify/publish semantics; confirmed live in production by the original audit's own independent function check.
- **Numeric Research Router v1** (π family → Fibonacci → PR #200/#203, and the later Gematria-Trace extension, 09-03) — a working research-routing capability, with at least one self-caught and fixed bug (the Fibonacci stress test).
- **A 35-item, method-by-method linguistic-cipher research pass on ספר הפליאה** (09-02) — real, systematic, individually-labeled research work, uniformly and explicitly marked "not canonical, not published" — this is careful research practice, not silent data dumping.
- **A working ELS build sprint** (08-23, PR #174→#185) delivering the Matrix Snapshot, Native Renderer, Hidden Engine Bridge, Verse/Context Lens, and Research Journey Core.
- **Several small, correctly-scoped fixes shipped same-day**: Atbach method definition correction, Gematria Method Readiness cleanup, System-Map visual-progress fix, Codes Reference Surface terminology fix.
- **Ongoing Command Center UI exploration** (09-03/09-04: Command-Room Simple Workdesk, Zvi Golden-Case, One-Tree projection, source-map reference, Human Gate Desk v1) — all explicitly branch-preview, not merged, actively iterating toward resolving what the original audit called "Gate #9."
- **At least one self-imposed stop**: the Portable Research Context closure attempt on 09-01, abandoned mid-write because "no safe byte-preserving write" existed — direct evidence the workflow declines unsafe writes rather than forcing them through.

## 4. What Claude Already Knew (and reported correctly the first time)

The original audit's own **docs-claims-extraction pass** (Section B of the first audit) already read and correctly summarized `research-studio-v1-contract.md`, `research-universal-finding-contract.md`, `corpus-admission-foundation-v1-contract.md`, and Master State §0-A — including the exact "One Reality Graph / one Research Workspace / one Human Gate" sentence, the three-storage-homes rule, and the "no automatic `research_object` creation per Finding" rule. That pass **did not** claim these were mysterious. The error was introduced later, in the separate **Supabase ground-truth pass**, when it saw 15 live `work_log_current` rows using this vocabulary and concluded — without cross-referencing back to its own docs pass — that it was looking at something the documented architecture didn't name. It was the same architecture; the two passes simply weren't reconciled with each other before the first report was written.

## 5. What Claude Mistook for Unknown/Parallel — OLD → NEW

Re-judging the original audit's Sections C, D, H, K, O, Q. Verdict key: **STILL TRUE** (real gap even after GPT's work) · **EXPLAINED BY GPT WORK** (not mysterious — documented GPT activity) · **STALE DOCUMENTATION** (implementation moved, docs lagged) · **REAL CONFLICT** (GPT and Claude actually built competing things).

| # | OLD INTERPRETATION (first audit) | NEW INTERPRETATION | Verdict |
|---|---|---|---|
| 1 | "A parallel, undocumented GPT+ZURIEL operating system (Research Objects/Human Gate/Truth Axes/PR workflow) is the actual live mechanism shipping features today — CLAUDE.md doesn't name it." | It is the same documented Research OS (PR #187, merged 08-24, already in Master State §0-A) being actively operated by GPT day-to-day, with a handful of genuine but explicitly-labeled extensions (Truth Axes law, identity-invariant law) layered on top — each self-described as "extends X, does not rewrite." CLAUDE.md not naming the *day-to-day operational cadence* is a real documentation gap, but the *architecture* was never undocumented. | **EXPLAINED BY GPT WORK**, with a residual **STALE DOCUMENTATION** note: CLAUDE.md's `agent_onboarding_law`/`active_systems_map` should name this operational cadence explicitly so a fresh agent doesn't repeat this error. |
| 2 | "git history reset (2026-09-01) orphaned 233 of 274 branches; 106 carry real content never reconciled." | The reset and the branch count are unchanged facts. What's revised: "never reconciled" overclaimed. A 13-branch sample showed most flagged branches are **superseded by equivalent work delivered via different commits** (confirmed for `els-work-area`, `els-foundation-integration`, `command-center-3d-roadmap-parser`, `roadmap-v4`) or **partially overlapping** (library ported, UI not — `els-research-journey-v2`; DB migrated, UI not — `command-center-vnext-projection`). Only 2 of 13 sampled are genuinely, uniquely absent (`ahavat-torah-full-book-inventory`, `letter-parasha-reconstruction`, plus the application layer of `raziel-capabilities-audit-h5k9ww`). | **STILL TRUE that the reset happened and branches are orphaned from git's perspective; STALE/OVERCLAIMED that most of them represent lost work** — see §8 for the corrected per-branch disposition. |
| 3 | "`gematria-lists-organization-u39nlj` (101,682 lines) is the single largest stranded artifact — worth a deliberate decision, possibly lost work." | The branch's own `README.md`/`MANIFEST.md` explicitly label `MASTER_CLASSIFICATION.csv`(v1) and `_v2.csv` as **SUPERSEDED**, and `_v3.csv` as **SCRATCHPAD_ONLY / candidate, unreviewed** — this was never ambiguous, it was self-documented as non-canonical the day it was created. A same-day `work_log` memo states explicitly "0 DB writes performed except this memo." `gematria_words` (the live table) grew every week after this branch's date with zero dependency on it. | **STALE/OVERCLAIMED — corrected**: this is exactly what a well-labeled research-preservation snapshot should look like. No action needed. ZURIEL's original framing (v1/v2 superseded, v3 = snapshot, live table independent) is confirmed **exactly correct**. |
| 4 | "`claude/video-transcription-multilingual-neqwjm` — the most likely genuinely-stranded real work (last commit one day before the audit), worth immediate manual review." | 8 of 9 checked components/docs from this branch are **already present, byte-identical, in current `origin/main`** — delivered via separate commits over the following two weeks (`HumanDateInput.jsx`, `ContributorStudio.jsx` minus one line, `AskRaziel.jsx`, `DimensionFiveCloud/Rail.jsx`, the engine-governance docs, and the SQL/RPC layer are all live). **Exactly one item is genuinely missing**: a single-line Hebrew brand-spacing fix (`"סוד1820"` → `"סוד 1820"`) in `ContributorStudio.jsx`, plus its accompanying `brand_spelling_law` rule — which is also **absent from current `CLAUDE.md`**, i.e. it regressed at the documentation layer too. | **STILL TRUE, narrowly**: one real, small, easily-reapplied fix is missing. **STALE/OVERCLAIMED** that this was "the most likely genuinely-stranded real work" at scale — it wasn't; almost all of it already shipped. |
| 5 | "Half of CLAUDE.md's 'canonical UI components' (ToolActions/UserBadge/ResearchChip/AIAnalysisCard) don't exist in code at all." | Not touched by any GPT work in this window — GPT's entire Aug 12–Sep 4 footprint is backend/DB/research-data/docs, essentially zero frontend component work in the ledger. | **STILL TRUE** — unaffected by this reconciliation. |
| 6 | "Command Center CC-1's official target is still undecided between two live dashboards (WarRoomTab.jsx vs RoadmapCommandCenter.jsx) — Gate #9 open." | GPT is actively iterating toward a resolution: 5 separate "Command-Room" branch-preview builds landed 09-03/09-04 (Simple Workdesk v1, Zvi Golden-Case, One-Tree projection, source-map reference, Human Gate Desk v1), all explicitly unmerged, all exploring the same problem space. This is a gate genuinely still open, but it is **active, current work-in-progress**, not neglect. | **STILL TRUE that Gate #9 is open**, but the framing shifts from "unresolved ambiguity" to "unresolved-because-still-being-actively-explored, decision point is imminent and belongs to ZURIEL" (see §11). |
| 7 | "A parallel Gemini AI track runs by default in at least one AI touchpoint, undisclosed in CLAUDE.md's Claude-only framing." | Not investigated in this reconciliation round (out of scope — this was a frontend `lib/supabase.js`/`lib/cost.js` finding, unrelated to GPT's DB/research work). | **STILL TRUE, not re-examined** — carried over unchanged. |
| 8 | "63% of number nodes (1,332/2,122) have zero graph edges." / "`convergences` RLS-enabled-zero-policy, zero edges to graph." / "`fn_mem_add`/`fn_raziel_fact` missing ownership check." | Not investigated in this reconciliation round (graph/RLS/security topics, unrelated to the GPT-chronology question). | **STILL TRUE, not re-examined** — carried over unchanged. |
| 9 | "A whole undocumented `research` Postgres schema runs an independent Tanach/hypothesis/operator research engine (116,761-row `transform_pairs`), RLS disabled entirely." / "A 220K-row identity-resolution subsystem (persons/identity_edges/sod_id_registry) exists with no product surface named anywhere." / "Undocumented Vault secret `OPENWEB_SSO_SHARED_SECRET`." | Not investigated in this reconciliation round — none of the 4 passes queried the `research` schema, identity tables, or vault secrets. **This is a genuine gap in this reconciliation, not a resolved item** — it needs its own follow-up pass before any verdict is assigned. | **STILL TRUE, EXPLICITLY UNRESOLVED** — do not treat as either confirmed-parallel or explained; it is simply outside this round's scope. |
| 10 | "ELS's own 'add to research' button has never once written a row to `research_objects`." | Confirmed independently by the `research_objects` classification pass: only **8 of 680 rows** (1.2%) are ELS-sourced (`pre_els_finite_closure`), and none show the ELS UI's own add-to-research action as their origin. | **STILL TRUE** — the ELS↔Research-Bus bridge gap is real and unaffected by GPT's other work. |
| 11 | "`research_objects` growth (124→680, implicitly framed as 'very active' but unexplained)." | Fully explained: 3 concentrated bursts — Aug 26 (+345, Zvi's personal-notebook extraction + Amit's corpus stress-test, both documented, no duplicates found), Sep 2 (+68, of which 42 are the labeled Sefer HaPeliah cipher-method sweep), Aug 24 (+38, Research Studio rollout). **Critically: 0 of 680 rows have `promoted_node_id` set** — none have actually crossed the Human Gate into the canonical graph. 672/680 (98.8%) remain `status='candidate'`. | **EXPLAINED BY GPT WORK** — well-documented, traceable, non-anomalous bulk onboarding under explicit "stress test" protocols, with an important nuance: it's growth of the *candidate pool*, not of canonical knowledge. |
| 12 | "Two competing, non-overlapping research lineages on the same topic (Claude's Ahavat-Torah book-inventory line vs GPT's research-ledger v1-v7)." | Confirmed as real and still unreconciled — but now understood in context: GPT's v1-v7 chain is a linear self-superseding checkpoint sequence (each explicitly a "session-exit checkpoint," not a competing corpus), and Claude's line produced separate, real, git-only JSON datasets that never reached any DB table. Both are legitimately "stress tests" per Master State §23.26 — the *reconciliation gap* is real, but it is not evidence of conflict, just of two independent research threads on the same book that were never diffed against each other. | **STILL TRUE that reconciliation is needed**, reframed from **REAL CONFLICT** to **coexisting, non-conflicting, unreconciled research threads** — a scope decision for ZURIEL (§11), not a bug. |
| 13 | "Two competing DRAFT rewrites of the Master Roadmap itself sit unmerged and unreviewed (`raziel-capabilities-audit-h5k9ww` 'v3 DRAFT', `roadmap-v4` 'v4 DRAFT')." | `roadmap-v4` confirmed **superseded-by-equivalent-work**: the live Roadmap reached v5.3 through an entirely different, much later lineage of edits — the roadmap-as-artifact function is fully served today, just not by this draft's text. **`CLAUDE.md` itself still says the canonical Roadmap is "v4"** (`ssot_terminology_routing_law`: "תמיד SOD1820_MASTER_ROADMAP.md (v4, קנוני)") — this is now a confirmed, dated documentation-drift bug: CLAUDE.md's own pointer is one major version behind live main. | **STALE DOCUMENTATION — newly confirmed, concrete**: CLAUDE.md needs its Roadmap version reference corrected from v4 to v5.3 (a content-authority change, flagged for ZURIEL per the "no CLAUDE.md change yet" rule in this round). |

## 6. Real Remaining Architectural Gaps (post-reconciliation)

These held up under scrutiny and are unaffected — or only lightly reframed — by GPT's documented work:

- Missing canonical UI components (`ToolActions`, `UserBadge`, `ResearchChip`, `AIAnalysisCard`) — zero GPT activity touched frontend components in this window.
- Command Center CC-1 target — open, but now known to be under **active** exploration (5 branch-preview iterations in the last 48 hours of the window), not stalled.
- ELS → `research_objects` bridge — still effectively unused (8/680 rows, 1.2%).
- `researcher_definitions` (ZURIEL's own agent-instruction channel) has had **zero new entries since 2026-07-23** — a genuinely new finding from this reconciliation round, not previously flagged: ZURIEL's direct-to-agent definitions channel has gone quiet for six weeks while a large amount of architecture work happened through PRs and `work_log` instead. Worth confirming this is intentional.
- 672 of 680 `research_objects` rows (98.8%) remain uncanonicalized `candidate` status, and 0 have ever been promoted (`promoted_node_id`) — the Human Gate exists and is used for code/schema releases, but the *research-content* backlog behind it is large and untouched.
- Everything in row 9 of the table above (`research` schema, identity-resolution subsystem, `OPENWEB_SSO_SHARED_SECRET`) — explicitly not re-examined, still open.
- The Gemini-default-engine disclosure gap, the `fn_mem_add`/`fn_raziel_fact` security gap, the graph-orphan/RLS findings — explicitly not re-examined, still open.

## 7. Books/Research Changes Specifically

- **Ahavat Torah**: only **2** `research_objects` rows exist (both session-checkpoint placeholders, private/candidate) — the *substantial* research content (full-book inventory, letter×parasha reconstruction — real, large, git-committed JSON datasets, 4,502 and 3,181 lines respectively) lives **only** on unmerged `claude/ahavat-torah-*` branches and was never written to any DB table. This is a genuine gap worth ZURIEL's attention (§11) — not because it's "lost" (it's safely committed and readable via `git show`), but because it currently has no path into the Research OS at all.
- **Sefer HaPeliah**: **42** `research_objects` rows, all traced to the single, well-documented 09-02 cipher-method sweep — every row explicitly `candidate`, `not canonical`, `not published`. This matches the original audit's framing closely; no revision needed here.
- **GPT's `ahavat-torah-research-ledger` v1→v7 chain**: a linear, self-superseding sequence of session checkpoints (not 7 competing corpora) — v7 is simply the latest state of one evolving thread, distinct from Claude's separate JSON-dataset thread above. Neither thread references the other.
- **`corpus_admission_foundation_v1`**: governs `gematria_words` intake specifically, not book/manuscript research — confirmed zero overlap with the Ahavat Torah/Peliah work (0 rows in `research_objects` reference `corpus_admission` by name).

## 8. Branches: Archive / Inspect / Truly Recover

| Branch | Disposition | Why |
|---|---|---|
| `claude/gematria-lists-organization-u39nlj` | **ARCHIVE — no action needed** | Self-labeled scratchpad/superseded snapshot; live table demonstrably independent. Confirmed correct as-is. |
| `claude/roadmap-v4` | **ARCHIVE** | Superseded by the live Roadmap's own v5.3 lineage via different commits. |
| `claude/els-work-area` | **ARCHIVE** | `/lab/els` shipped via a different commit (`3e77f15a`) same week. |
| `gpt/els-foundation-integration` | **ARCHIVE** | Superseded by the later, more complete `docs/els-capability-audit.md` + `docs/els-capability-workarea-unification.md`. |
| `claude/command-center-3d-roadmap-parser` | **ARCHIVE** | Explicitly cited as its own predecessor in main commit `931e72f7`'s own message ("previously an isolated side-project on..."). |
| `claude/video-transcription-multilingual-neqwjm` | **ARCHIVE, with one TRULY RECOVER item** | 8/9 checked pieces already live via other commits. **Recover:** the one-line `"סוד1820"` → `"סוד 1820"` fix in `ContributorStudio.jsx` and its `brand_spelling_law` rule (currently absent from `CLAUDE.md`). Small, concrete, zero ambiguity. |
| `claude/els-unified-merge`, `gpt/els-research-journey-v2` | **INSPECT** | Partially overlapping: `elsJourney.js` library was ported into main verbatim (comment confirms it), but the UI wiring that consumes it in `ElsWorkAreaPage.jsx` was not. Worth a scoped follow-up to finish wiring the already-ported library, not a branch merge. |
| `gpt/command-center-vnext-projection` | **INSPECT** | DB migrations already merged via PR #277; `CommandCenterNextPage.jsx`/`ResearchViewerLegacy.jsx` (the UI) were not. Same pattern as above — DB is done, UI decision is pending. |
| `claude/ahavat-torah-full-book-inventory`, `claude/ahavat-torah-letter-parasha-reconstruction` | **INSPECT — real, unique, undecided** | Genuinely absent from main and from `research_objects`; real research content (JSON datasets) sitting only in git. Needs a ZURIEL decision on whether/how to bring this into the Research OS (§11). |
| `claude/raziel-capabilities-audit-h5k9ww` | **INSPECT — most substantial genuinely-stranded item found** | `fieldpackage.js`/`inforequest.js`/`ccnav.js`/`field-pack` Edge Function application layer confirmed absent from main via any commit. Only the underlying `fn_gematria_pack` DB primitive is live. Needs a scoped decision on whether this feature is still wanted. |
| Everything else from the original audit's "106 orphaned branches with real content" list | **Downgrade prior urgency** | The sampling above (13 branches) found a 2:1 ratio of superseded-or-overlapping vs. genuinely-unique-and-absent. Treat the remaining un-sampled branches with the same presumption — check before declaring "at risk" — rather than assuming loss. |

## 9. Documentation Drift (confirmed this round)

- **`CLAUDE.md`'s `ssot_terminology_routing_law`** still states the canonical Roadmap is **"v4"**; live `origin/main`'s Roadmap is at **v5.3**. Concrete, dated, easily fixed — but per this round's hard rules, not fixed here; flagged for ZURIEL.
- **`brand_spelling_law`** (the "סוד 1820 with a space" rule) exists on the stranded `video-transcription-multilingual-neqwjm` branch but is **absent from current `CLAUDE.md`** — a rule that regressed, not just failed to propagate forward.
- The four newer canonical docs from 2026-09-04 (`research-os-canonical-lock-v1.md`, `sod1820-system-frame-contract-v1.md`, `sod-knowledge-navigation-map-v1.md`, `system-map-gate.md`) identified in the original audit are still not folded into `CLAUDE.md` — unchanged finding, still true.
- `CLAUDE.md`'s `agent_onboarding_law`/`active_systems_map` do not name the GPT operational cadence (work_log-driven PR sprints, the Truth Axes/corpus-admission rule family) at all — this is the one real documentation gap behind the original audit's "parallel system" misread, and is worth closing explicitly (see Top-10, §10).

## 10. Revised Top-10 Actions

1. **Update `agent_onboarding_law`/`active_systems_map` in `CLAUDE.md`** to name the GPT-driven Research OS operational cadence (PR-gated, `work_log`-tracked, Truth-Axes-governed) explicitly, so no future agent re-derives "parallel system" from a partial read of `work_log_current`.
2. **Correct the Roadmap version pointer in `CLAUDE.md`** from "v4" to "v5.3" (`ssot_terminology_routing_law`).
3. **Reapply the one confirmed-missing item** from `video-transcription-multilingual-neqwjm`: the `"סוד1820"`→`"סוד 1820"` fix plus its `brand_spelling_law` rule.
4. **ZURIEL decision on Ahavat Torah's stranded datasets** (§11) — formally ingest, or consciously leave as git-only research artifacts.
5. **ZURIEL decision on the CC-1 Command Center target** (§11) — the 5 recent branch-preview iterations suggest a decision point is close; make it explicit rather than letting a 6th prototype appear.
6. **Finish wiring the two DB/library-done-UI-pending items**: `elsJourney.js` into `ElsWorkAreaPage.jsx`, and the Command-Center-vNext tables into a UI (or confirm neither is wanted).
7. **Confirm whether `researcher_definitions` going quiet since 2026-07-23 is intentional** — if ZURIEL now prefers `work_log`/PR-based communication, say so explicitly rather than leaving an apparently-abandoned channel.
8. **Schedule a Human Gate review pass over the 672 candidate `research_objects` rows** — not because anything is wrong, but because a growing 98.8%-candidate backlog needs a plan (batch review vs. domain-by-domain vs. leave as a permanent research pool).
9. **Still-open, carried over from the original audit, unaffected by this reconciliation:** build or retire the missing canonical UI components; fix the `fn_mem_add`/`fn_raziel_fact` ownership-check gap; resolve the `convergences`/`project_codex` RLS-no-policy situation; disclose the Gemini-default-engine reality.
10. **Open a dedicated follow-up pass on the `research` Postgres schema and the identity-resolution subsystem** (`persons`/`identity_edges`/`sod_id_registry`) and the `OPENWEB_SSO_SHARED_SECRET` — explicitly not resolved by this reconciliation, and should not be assumed either "fine" or "parallel" until actually investigated.

---

## 11. WHAT ZURIEL ACTUALLY NEEDS TO DECIDE

Real human decisions only — nothing below is something an agent should resolve unilaterally.

1. **Ahavat Torah datasets** — the full-book inventory and letter×parasha reconstruction (real research, currently git-only, never reached any DB table): formally bring into the Research OS as a new intake batch, or deliberately leave as a standing git-only research reference? Either is legitimate; it's a scope call.
2. **Two Ahavat-Torah research threads (yours-via-Claude vs. GPT's v1-v7 ledger)** — reconcile into one, or keep as intentionally-independent parallel angles on the same book? No conflict was found, but no reconciliation exists either.
3. **Command Center CC-1** — which artifact is the real one: `WarRoomTab.jsx`, `RoadmapCommandCenter.jsx`, or one of the five newer Command-Room prototypes from 09-03/09-04? This is Gate #9, and it's the one gate this reconciliation found to be under genuinely active, converging exploration — a decision now would land well.
4. **`raziel-capabilities-audit-h5k9ww`'s Field Package / Info Request UI layer** — still wanted, or officially retired? It's real, substantial, and currently has no owner or decision either way.
5. **`researcher_definitions` going quiet since 2026-07-23** — intentional shift to work_log/PR-based communication, or an oversight you'd like reopened?
6. **The 672-row `research_objects` candidate backlog** — leave it as an ever-growing candidate pool, or schedule a deliberate Human Gate review pass (and if so, what cadence/ownership)?
7. **`CLAUDE.md`'s Roadmap version pointer (v4 → v5.3) and the missing `brand_spelling_law`** — these are content-authority edits to a file this round was explicitly told not to touch; approve the two specific corrections named in §9/§10 so an agent can make them next session.
8. **The `research` Postgres schema and identity-resolution subsystem (`persons`/`identity_edges`/`sod_id_registry`)** — is this GPT's legitimate private research infrastructure that should simply be documented as such, or does it need architectural reconciliation with the main graph? This reconciliation round deliberately did not investigate it enough to recommend either way — it needs your framing before an agent goes further.

---

*Prepared on branch `claude/sod1820-full-system-audit-8jcfdg`. No merge, no PR, no deploy, no CLAUDE.md edit, no branch recovery, no RLS change was performed.*

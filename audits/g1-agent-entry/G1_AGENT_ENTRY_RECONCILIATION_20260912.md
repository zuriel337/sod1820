# SOD1820 — G1 AGENT ENTRY / BOOTSTRAP RECONCILIATION 2029

Date: 2026-09-12  
Canonical Supabase: `linswmnnkjxvweumprav`  
Baseline `origin/main`: `96c32dade94814a2ff079206cd606d0f16522bf3`  
Primary owner: `inter_agent_coordination_law v9`  
Foundation gate owner: `foundation_closure_protocol_law v4`  
Owner check: `EXTEND_EXISTING`  
Primary writer: GPT  
Specialist: CLAUDE = REQUIRED / READ_ONLY  
Release authorization: **NONE**

## 1. G1 acceptance contract

G1 does not create a Context System, registry, store, router, prompt SSOT, memory authority, or agent-specific owner family.

Default task entry is:

`natural-language task → intent/capability/domain → Task Profile when useful → current canonical owner → minimum direct dependencies under Read Budget → recent relevant CURRENT work_log/assignment context → appropriate live verification → execute → verify/handoff`

Full Master State, full Roadmap, all active rules, all `project_codex`, broad branch archaeology and unrelated contracts are not default startup reads.

Authority remains separated:
- live DB + `origin/main` + relevant Production = live reality/evidence;
- current domain owner = semantics;
- Master State = documented state;
- Roadmap = navigation/priority;
- work_log = coordination/provenance;
- Owner Index = routing map;
- adapters / codex pointers / prompts / memory / conversation = context or pointers only.

## 2. Work-log scope / provenance

Execution BEFORE: `4108ff0f-f67b-45fd-a626-fdbeade15b13`  
Claude READ_ONLY assignment: `9c65facf-a87d-4883-8fb2-9939759a31aa`  
Fresh GPT READ_ONLY assignment: `edb691dc-d359-4672-bdaf-e681044a84ba`

At the latest pre-write scan there was no overlapping G1 writer. Claude specialist AFTER had not yet appeared. The current contextual GPT session is explicitly barred from self-certifying the fresh-GPT assignment.

## 3. Active entry-surface reconciliation matrix

| Surface / runtime | Live/main observation | Decision | Current state |
|---|---|---|---|
| `SOD1820_MASTER_OWNER_INDEX.md` | Already owner-first, minimal-read, v9-aware; routing-only and explicitly not a new SSOT | KEEP | **PASS — MAIN** |
| `CLAUDE.md` | Main adapter contained mandatory broad startup chain, copied domain semantics/current snapshots, pinned/stale operational state and multiple “read before everything” instructions | Reduce to runtime mechanics + pointers; remove copied domain authority while preserving history in git | **FIXED — BRANCH CANDIDATE** |
| `AGENT_HANDOFF.md` | Main declared an 8-source mandatory bootstrap including full Master/Roadmap/rules/codex | Demote to on-demand coordination/provenance pointer | **FIXED — BRANCH CANDIDATE** |
| `.claude/settings.json` | Only tool-permission allowlist; no startup prompt, owner hierarchy, memory authority or hidden bootstrap content | KEEP | **PASS — MAIN / NO HIDDEN ENTRY CONTRACT** |
| `project_codex._index` | Live row instructed full/sequential codex reading, raw work-log reading and mandatory Master/Roadmap/CLAUDE startup; authority order mixed navigation/docs with semantic/live authority | Bounded live pointer correction only; no new row/system/owner | **FIXED — DB LIVE** |
| GPT project bootstrap / custom project instructions | Current runtime contract observed owner-first, progressive disclosure, live verification and work-log coordination; it does not require full Master/Roadmap by default | KEEP; do not duplicate into repo | **CONFIG INSPECTION PASS; FRESH-SESSION REPLAY PENDING** |
| Raziel routing runtime | `fn_raziel_route` contains capability routing, permission scoping and expert selection; no project-wide Master/Roadmap/codex/work-log bootstrap references found | Treat as domain runtime, not project context authority | **RUNTIME BOUNDARY PASS** |
| Metatron context runtime | `metatron_context` builds DB/engine evidence context and active method rules; no project-wide Master/Roadmap/codex/work-log bootstrap references found | Treat as Research/System context runtime under existing owners, not project bootstrap authority | **RUNTIME BOUNDARY PASS** |
| Historical docs / archived prompts | Not active entry targets by default | Preserve history; no bulk cleanup | **OUT OF G1 BOUNDED SCOPE** |

## 4. Exact drift fixed

### D1 — Claude adapter startup authority

Main `CLAUDE.md` told every substantial task to read `AGENT_HANDOFF.md` immediately and reconcile a broad chain including rules/codex, full Master State and Roadmap before domain work. It also carried extensive copied domain semantics and current-state snapshots.

Candidate replaces it with a compact Claude runtime adapter that:
- points to current `inter_agent_coordination_law` and Owner Index;
- makes L1 owner + direct dependency the default;
- reserves L2/L3 for real cross-domain/write/risk needs;
- keeps only genuine Claude/git runtime mechanics locally;
- makes `AGENT_HANDOFF.md` on-demand rather than mandatory;
- forbids adapter/memory authority over live owners/state;
- preserves release/Human-Gate/one-writer safety;
- names the fresh-agent test explicitly.

History is preserved in git; no archive is rewritten.

A direct `.claude/` directory census found only `settings.json`, containing tool permissions only. No second Claude prompt/startup contract was found there.

### D2 — Handoff document as universal bootstrap

Main `AGENT_HANDOFF.md` imposed eight mandatory startup reads. Candidate now owns coordination/provenance mechanics only and points back to the active owner-first task-entry contract.

### D3 — Codex `_index` broad-read / authority drift

Live `_index` was corrected in place, preserving the same row and history semantics:
- codex is now explicitly a specs/context library, not mandatory bootstrap or parallel truth;
- agents are told not to read all codex sequentially;
- work_log is Coordination/Provenance and startup reads only relevant CURRENT rows;
- old “before every task read Roadmap + Master + CLAUDE” wording is removed;
- authority split now matches v9;
- mandatory broad bootstrap block is replaced by owner-first/minimal-dependency G1 task start.

Post-write checks confirmed the old broad-startup markers are absent and all three replacement markers are present.

## 5. Runtime replay evidence

### Raziel — deterministic route probes

Safe read-only probe: `בדוק גימטריה על 358`  
Result: `single_domain`; selected only canonical `gematria_engine`; effective permission remained public; no invented parallel expert/system.

Safe read-only probe: `בדוק דילוג אותיות למילה משיח`  
Result: capability-matched ELS/letter experts under the existing route contract; no project-document bootstrap path was introduced.

### Metatron — context probe

Safe read-only request for public Gematria context around `358` produced:
- one explicit target;
- canonical verified matches from DB;
- active capability list;
- active-method rules payload + context hash;
- no Master/Roadmap/codex/work-log project bootstrap dependency.

These probes establish the **runtime authority boundary** for Raziel/Metatron. They do not by themselves prove a fresh GPT/Claude project-agent session.

## 6. Branch / PR candidate

Branch: `gpt/g1-agent-entry-reconcile-v1`  
Draft PR: `#448 — G1 2029 — agent entry / bootstrap reconciliation`

Foundational commits:
- `90e0fdd99640b3535c152cb3ce71b46b76369a40` — compact Claude owner-first runtime adapter
- `db77bd8a2bdd7238ae1502129d97af034144ea2b` — bounded handoff/provenance pointer

Scope is intentionally only active entry semantics. No domain law, Product UI, G2 legacy knowledge, new registry/store/context system, or runtime schema was created.

## 7. Fresh-agent acceptance — current verdict

| Runtime / adapter | Static/config reconciliation | Runtime/task replay | Fresh session without prior project memory | Verdict |
|---|---:|---:|---:|---|
| Claude | candidate aligned; hidden `.claude/` prompt check clean | specialist assignment issued | **PENDING** | **NOT YET ACCEPTED** |
| GPT | current project bootstrap inspected aligned | fresh-session assignment `edb691dc...` issued; current session cannot count | **PENDING** | **NOT YET ACCEPTED** |
| Raziel | owners/routing checked | safe route probes passed | product runtime does not consume project bootstrap docs | **BOUNDARY ACCEPTED** |
| Metatron | owners/context function checked | safe context probe passed | product runtime does not consume project bootstrap docs | **BOUNDARY ACCEPTED** |

A static prompt containing the correct words is not proof. Per v9, G1 remains NOT SUFFICIENT until the materially different project-agent adapters receive actual fresh-session acceptance evidence and the Claude independent challenge is reconciled.

## 8. Open blockers before G1 closure

1. **CLAUDE AFTER PENDING** — independent READ_ONLY challenge must decide whether the compact adapter removed any genuinely runtime-specific Claude mechanic that materially affects safety/release/operation.
2. **CLAUDE fresh-session replay PENDING** — after candidate is available to a fresh Claude runtime, prove owner resolution + minimal reads + relevant work-log discovery + correct live verification without prior memory.
3. **GPT fresh-session replay PENDING** — assignment `edb691dc-d359-4672-bdaf-e681044a84ba` must be executed by a new GPT project session; this already-contextual session may not self-pass it.
4. Reconcile any decision-changing specialist delta on the same branch/owner; no parallel entry contract.
5. Exact-head CI + final main/DB/work_log reconciliation before release.
6. Merge only on explicit ZURIEL `תעלה`; then repeat closing acceptance against `origin/main` and live pointers before declaring G1 CLOSED.

## 9. Current exact state

**DB POINTER FIX: LIVE**  
**REPO ADAPTER FIXES: IMPLEMENTED · COMMITTED · BRANCH-ONLY · DRAFT PR #448**  
**G1: NOT SUFFICIENT · NOT CLOSED · NOT MERGED · NOT DEPLOYED**  
**G2: NOT STARTED**

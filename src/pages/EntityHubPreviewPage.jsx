import React from "react";
import NumberHubPage2029 from "./NumberHubPage2029.jsx";

// G3 Golden review route: preserve the familiar Number Page shell and rhythm,
// then expand it with the approved 2029 Number/Phrase capabilities underneath.
// Branch-only preview; production /number is unchanged until explicit Human-Gate release.
export default function EntityHubPreviewPage() {
  return <NumberHubPage2029 />;
}

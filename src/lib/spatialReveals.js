// RETIRED BY HUMAN GATE (2026-09-15): legacy spatial reveal specifications removed.
// Compatibility API intentionally returns no scene so old markers cannot resurrect retired UI.
export function getSpatialReveal() {
  return null;
}

export const SPATIAL_REVEALS = Object.freeze({});
